# 開発案メモ

2026-08-10 作成。**これは案の置き場であって、決定事項ではない。**
確定した仕様は `SPEC.md`、いまの実装の状態は `HANDOFF.md` を見ること。

ここに書いてあるものは**まだ実装しない。** 本人が「やる」と言ったものだけを
`SPEC.md` 側に移してから着手する。

---

## 案1：日記機能の追加 → **2026-08-13 に決定・実装ずみ（Phase 1）**

> **この案は着手ずみ。** 下の「検討メモ」は判断材料として残してあるが、
> **決まったことは「決定（2026-08-13）」の節に書いてある。そちらが正。**
> 実装の中身は `HANDOFF.md`「日記」を読むこと。

### 決定（2026-08-13）

本人が決めたこと：

| 論点 | 決定 |
|---|---|
| 入力欄の形 | **③ 既定は1欄、必要なときだけ他の欄を開く**（下の3案から選択） |
| 箱 | **消費しない。** やりたいことリストに入れるかどうかは本人の自由 |
| 画面 | **日記タブを新規追加。** 上に今日の入力欄、下に過去のもの |
| 書いていない年 | **表示しない。** あるときだけ出す |
| 置き場 | **localStorage のみ（Phase 1）。** 外部との連携は**あとから足す** |
| 暗号化 | **しない** |

**置き場について、この案を書いた時点から変わったこと：**

- `prune()` と容量の問題は、**本体とは別のキー `jiko-kanri-diary-v1`** に持つことで解決した。
  `migrate()` も通らないので、**データ版は上げていない（v7 のまま）**。
  → 下の表にある「日記を足すなら v6」は**不要になった**（そもそも v6・v7 は別の機能で使用ずみ）
- **Supabase を直に叩く案は採らない。** anon key が公開ソースに載るため。
  やるなら **Vercel の Function を挟んで鍵を隠し、さらに合言葉で入口を閉じる**（本人の提案）。
  ただし**鍵を隠すだけでは足りない**。無認証の Function は URL を知っていれば誰でも読める
- Phase 2（同期）は**着手しない**。足すときは `saveDiary()` の後ろに送信を挟むだけで済むよう作ってある
  → **2026-08-14 に決定した。** 日記だけの話ではなくなり（箱とやりたいことも同期する）、
  案ではなく仕様になったので、**`SPEC.md`「追加仕様：端末をまたいで使う」が正**。まだ未実装

### やりたいこと（本人の案）

- 入力欄を作って、その日のことを書けるようにする
  - 今日できたこと
  - 今もやもやしてること
  - 吐き出したいグチ
  - などなど
- **日記だけを見返せる**ようにする
- **昨年（またはそれ以前も全部）の同じ日の日記**も一緒に表示する

### 精査したいこと（本人が挙げた論点）

**何の欄を作るか。**

- 毎回ぜんぶ書く必要はない（書けない日に書かせない）
- ただし**ただの自由メモ欄だとジャーナリング効果が弱そう**

→ この2つの間のどこに置くかを決めたい。

### 必要な他のサービス

- **DB連携が要るかもしれない。**
- 別のアプリで **Supabase** を使っているので、Supabase でやるのがやりやすいかも。

---

## 検討メモ

以下は Claude 側の補足。**決定ではない。判断材料として置いておくだけ。**

### いまの作りとぶつかる点

| 点 | 内容 |
|---|---|
| **`prune()` で消える** | いまは `days` を **180日**で古いものから捨てている（`HANDOFF.md`「データ」）。「去年の同じ日」を出すなら、日記は `prune()` の対象外にするか、`days` とは別のキーで持つ必要がある |
| **localStorage の残り容量** | 上限の目安は 5MB。ごほうびの画像がすでに最大 3.2MB を使う。日記は文字なので1日500字でも年 400KB 弱だが、**何年も貯めるなら画像と食い合う**。「消えない前提で貯める」なら外部保存を先に決めたほうがいい |
| **`file://` で動くこと** | いまの必須条件。外部サービスを使うなら、`file://`（オリジンが `null`）から通るかを先に確かめる |
| **ライブラリなし・`type="module"` 禁止** | Supabase の JS クライアントはこの縛りと相性が悪い。使うなら **REST（`fetch` を直に叩く）** で書くことになりそう |
| **データ版を上げる** | いま v5。日記を足すなら v6 と `migrate()` の追加が要る |
| **日付キー** | 日記の日付も**午前2時区切り**（`dayKey()`）に揃える。深夜1時に書いたものは前日の日記 |

### 「消えないこと」をどう担保するか

日記は、やりたいことの記録と違って**過去に戻って読むことが目的**になる。
`HANDOFF.md` にある「**常に最新の状態が保存されていればよい。過去のデータに戻す要件は無い**」
という前提が、日記だけ当てはまらない。ここは先に決めておきたい。

考えられる置き場：

1. **localStorage のまま**（いちばん簡単・オフラインで動く／消えるリスクと容量が不安・PC1台に閉じる）
2. **Supabase に置く**（消えない・端末をまたげる／ネットが要る・`file://` から通るかの確認が要る・鍵の持たせ方を決める要あり）
3. **localStorage を正として、Supabase へ書き出すだけ**（普段の動きは変えずに保険をかける／二重管理になる）

> 既存のバックアップ（JSONの書き出し／読み込み）でも当面はしのげる。
> ただし手動なので「書き出し忘れた分は消える」。

### 入力欄の案（たたき台。どれも未決）

「毎回全部は書かせない」と「自由メモにしない」を両立させる方向として：

- **欄は固定で3つ置くが、全部任意。** 空のまま保存できる
  - できたこと／もやもや／グチ
  - 見出しがあるだけでジャーナリングの型にはなる。書けない日は空でよい
- **1日1問だけ出す。** 質問をローテーションさせて、答える欄はひとつ
  - 迷いが減る。ただし「今日はグチを書きたい」に応えられない
- **既定は1欄、必要なときだけ他の欄を開く**（折りたたみ）
  - 上2つの中間

あわせて決めること：

- 箱を消費するのか、しないのか（ルーティーンと同じ「消費しない」枠が素直そう）
- 書くきっかけをどこに置くか（箱を使い切って「今日は終了」になった所が自然？）
- 「日記だけ見返す」画面は、**タブを増やす**のか、いまの4タブのどこかに入れるのか
  （`SPEC.md` は「1画面で完結」「タブを行き来させない」方針なので、ここは要相談）
- 去年の同じ日を出すとき、**その日に書いていなかった年**はどう見せるか

### 決める順番（提案） → 4まで完了

1. ~~入力欄の形（上の3案から選ぶ）~~ → **③に決定**
2. ~~置き場（localStorage か Supabase か、併用か）~~ → **localStorage（別キー）。同期はあとから**
3. ~~見返す画面の置き方~~ → **日記タブを新規追加**
4. ~~実装~~ → **2026-08-13 済（Phase 1）**

---

## 案2：同じやりたいことを、同じ日に複数回（2026-08-19 相談）

> **まだ決定ではない。実装もしていない。** 本人が案を選んだら `SPEC.md` に移して着手する。

### 本人の言葉

- 同じリストを同日に複数回できるようにしたい
- 一度箱に入れたものを複製できる機能を検討したい

### 先に事実：足りないのは「箱」のほうだけ

**「1日2回」という頻度は、すでに動いている。**

| 場所 | いまの状態 |
|---|---|
| 頻度の登録 | `freq = { unit:"day", count:2 }` で「1日2回」と出る（`freqLabel()`） |
| 数え方 | `statOf()` が「今日 1/2」と数える。2回押すまで `met` にならない |
| 今日やったこと | 同じ日に2回押すと「2回」のタグが出る（`renderDoneToday()`） |
| **箱** | **1日に1つしか置けない** |

つまり**記録の側はもう複数回に対応していて、箱の側だけが1回きりになっている。**
本人の言う「複製」は、この箱の側を開ける話。

### なぜ箱は1つしか置けないのか（根っこは2つ）

#### 根っこ1：箱の中の「1つ分」に名札が無い

`d.slots` は **やりたいことのID をそのまま**並べた配列（`["tk1","tk1",null,"tk2"]`）。
1つ分を指す名前が無く、**IDそのものが名札**になっている。だから：

- `placeTask()` は `d.slots.includes(id)` で「すでに箱に入っています」と断る
- `placeTaskAt()` は置く前に**同じIDを全部どける**（＝ドラッグは「移動」であって「複製」にならない）
- `unplace(id)` は**同じIDを全部**出す（＝2つ置けたとしても、✕ で両方消える）
- `fitRange()` は同じIDの箱を**先頭から末尾までひとまとめ**に見る（＝間の別の箱ごと飲み込む）

> **いちばん危ないのは `boxGroups()`。** 隣り合う同じIDをくっつけるので、
> `[X, X]` が「**2箱の X**」なのか「**1箱の X が2つ**」なのか**区別できない**。
> ここだけはエラーも出ずに、見た目が黙って化ける。名札を分けずに複製を足すと必ず踏む。

#### 根っこ2：「終わったか」を箱ごとに持っていない

`doneOn(t, key)` は「**その日に log があるか**」の真偽値でしかない（`app.js` 内 10 か所で使用）。
1つ終わらせると、**同じやりたいことの箱が全部いっぺんに「終わった」になる**。

- `usedBoxes()` が両方を数える → 残り箱・「今日は終了」・空き箱の膜の濃さが全部ずれる
- `pendingGroups()` から両方が消える → 2つ目が「いま、これ」に上がってこない
- `renderBoxes()` / `renderWeek()` の行が両方 ✓ になる

### 直す必要がある場所（調査ずみ）

`slots` を読み書きしている箇所 **約30**、`doneOn()` **10**。うち作りに関わるのは：

| 場所 | いまの前提 | 複製を許すと |
|---|---|---|
| `boxGroups()` | 隣の同じIDは同じ1件 | **区別がつかない（要・名札）** |
| `placeTask()` / `placeTaskAt()` | 1日に1つ | 2つ目を置けるようにする |
| `unplace()` / `unplaceSilent()` | ID単位で全部どける | **どの1つか**を指定する |
| `fitRange()` / `setPlacedSize()` / `sizePickId` | ID単位 | 位置単位にする |
| `doneOn()` → `usedBoxes()` `pendingGroups()` `renderBoxes()` `renderWeek()` | その日やったか（真偽） | **何回やったか**（数）にする |
| `unplacedTasks()` | 置いたら右の一覧から消える | 触らなくてよい（下記） |
| `plannedAhead()` | **日**単位で数える（同じ日に2つ置いても1） | 触らなくてよい（下記） |
| `doTask()` の `planned` | `slots` 内の同IDの数 | その1つ分の箱数にする |

> **`unplacedTasks()` と `plannedAhead()` を触らずに済むのが大事。**
> 触ると `SPEC.md`「達成の数え方：行動ベース」の式そのものに手を入れることになる。
> **複製は「箱に入れたものを、今日ぶんだけ増やす」操作に限る**と決めれば、この式は無傷で通る。

### 案

#### 案A：複製＝別のやりたいことを作る（最小変更）

「散歩」を複製したら、**別IDのやりたいこと**を新しく作って箱に入れる。

- ○ 作りに一切触らない。✓・✕・箱の数・実績、全部そのまま効く
- ✕ **やりたいこと一覧に増えて、翌日も残る。** 今日だけの話にできない
- ✕ **記録が2つに割れる。** 「週3回」の集計も 🔥 も実績も別勘定になり、意味を失う
- ✕ リンクも複製される。あとで直すと片方だけ古くなる

→ **「同じことを2回やった」ではなく「似た別のもの」になってしまう。** 本人の言葉とずれる。

#### 案B：箱の名札に連番を足す（推奨）

`d.slots` の中身を `"tk1"`（1つ目）／`"tk1#1"`（2つ目）… にする。
**日記の保存キー（`"2026-08-15#1755230400000"`）と同じ手**。

- `baseId(x) = String(x).split("#")[0]` を1つ作って、`slots` を読む所を全部そこに通す
- **古いデータはそのまま読める。**「`#` が無い＝1つ目」なので、移行も**データ版の上げも要らない**
- **同期もそのまま通る。** `day` は中身を見ずに丸ごと預けているだけ（`applyRecord()`）
- `boxGroups()` は文字列が違うので**自然に別の塊に割れる** → 根っこ1が消える
- 「終わったか」は**順番で決める**：その日の log が n 回なら、**上から n 個目までの塊が終わり**
  - 保存するものが増えない＝同期の突き合わせも増えない（log の足し算のまま）
  - ✓ を押すと log が1つ増えて、上から順に埋まる。↩ で1つ戻る

| | |
|---|---|
| ○ | 移行なし・データ版そのまま・同期そのまま。根っこ2つが同時に消える |
| △ | **どの箱の ✓ を押しても「上から順に」終わる。** 下の箱を先にやったときは、上の箱に ✓ が付く（合計は正しい） |
| ⚠ | **古い版のままの端末が `#` 付きの箱を黙って捨てる**（`getDay()` の掃除が `taskById()` で見つけられないため）。しかも同期でその日ごと相手にも返る。**PCとスマホの両方を新しい版にしてから使うこと** |

#### 案C：箱の1つ分をオブジェクトにする（正攻法）

`slots: [{ id, uid, done }]` にして、終わったかも1つ分ごとに持つ。

- ○ いちばん素直。押した箱がそのまま終わる（案Bの △ が消える）
- ✕ **データ版 v7 → v8 と `migrate()`**。`HANDOFF.md` にあるとおり、移行は実際に事故った経路
- ✕ `slots.includes(id)` の形が全部使えなくなる（約30か所を書き換え）
- ✕ **同期の `day` の中身が変わる。** 古い版の端末では箱が全滅する
- ✕ `done` を `day` 側に持つと、**last-write-wins の `day` と足し算の `log` で二重管理**になる。
  `HANDOFF.md`「取り消しが足し戻される」と同じ種類の事故を、新しく作ることになる

→ 得るものは「押した箱がそのまま終わる」だけ。**案Bの順番ルールで足りるなら、割に合わない。**

### 推奨：案B

#### 複製の入口（たたき台）

- **今日の箱の行、✕ の隣に `⧉`。** 押すと**すぐ下の空き箱**に同じものをもう1つ置く
  （空きが無ければ「空き箱がありません」。勝手に箱は増やさない）
- **右の一覧（箱に入れていないやりたいこと）は、いまのまま。** 置いたら消える
  → 「**一度箱に入れたものを複製する**」という本人の言葉のとおりに切ると、
  `remainingToPlan()` の式に触らずに済む
- 終わった箱の `⧉` も押せてよい（「もう1回やる」がいちばん自然な流れ）

#### やること（実装するとしたら）

1. `baseId()` を作り、`slots` を読む所を全部通す（`getDay()` の掃除・`placeTask*`・`plannedAhead` ほか）
2. `unplace` / `fitRange` / `setPlacedSize` / `sizePickId` を**位置（`from`）で扱う**ように変える
3. `doneOn()` の隣に `doneCountOn(t, key)` を足し、`usedBoxes` `pendingGroups` `renderBoxes`
   `renderWeek` を「上から n 個目まで」に変える
4. `⧉` ボタンと `duplicatePlaced(from)` を足す
5. 版を4か所そろえる（`HANDOFF.md`「バージョンと更新」）

### 決めてほしいこと

| | 論点 | たたき台 |
|---|---|---|
| 1 | **複製できるのはどれか。** 単発も？　くり返しも？ | どちらも。ただし単発は総回数（`goal`）を超えて押すと「やり切った」に落ちる |
| 2 | **頻度の回数を超えて置いてよいか。**「毎日1回」を今日2つ置く | 置いてよい。箱は本人が決めるもので、頻度は「最低これだけ」の目安 |
| 3 | **✓ の順番。** 下の箱を先にやったとき、上の箱に ✓ が付いてよいか | よい（案Bの △）。駄目なら案C |
| 4 | **複製の入口。** 箱の `⧉` だけでよいか。右の一覧からも2つ目を出せるようにするか | `⧉` だけ。一覧からも出せるようにすると、達成の数え方に手を入れることになる |
| 5 | **1週間ページでも複製できるか** | まずは今日タブだけ。要るなら後から同じ仕組みで足せる |
| 6 | **上限** | 1日・同じやりたいことにつき何個まで置けるか（無制限でよいか） |

---

## 案3以降

（追記していく）
