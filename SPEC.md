# 自己管理アプリ 仕様（箱モデル）

2026-08-09 時点で確定した仕様。実装前の合意事項として残す。

---

## 前提

- **PCで使う。** スマホ対応は考えない
- 自由時間 ＝ PCを触れる時間。「このアプリだけ見ておけばいい」状態を作るのが目的
- 大きな予定（家庭教師など）の詳細は外部で管理する。このアプリは**消費した時間の量だけ**を知る

---

## 1日の区切り

| 区分 | 時間 |
|---|---|
| 日付の切り替え | **午前2時** |
| 午前 | 2:00 – 14:00 |
| 午後 | 14:00 – 翌2:00 |

- 既存の「朝(4-12) / 夜(18-4)」は廃止し、この午前／午後に置き換える
- 深夜1時はまだ「前日の午後」。日付も箱数の調整もリセットされない

---

## 箱

### 定義
- **1箱 = 30分**。箱は時間そのもの
- **1箱に入るやりたいことは1つだけ**。小さい用事は「細々した用事」のようなやりたいことを1つ作ってまとめる
- 箱は**個数と順番だけ**を持つ。時刻は持たない（9:00-9:30 のような割り付けはしない）

### 箱数の決め方
- **基本値**を設定しておく（平日◯箱／休日◯箱）
- その日の箱数は基本値から自動で始まる
- `＋` `−` ボタンでその日だけ増減する。翌日は基本値に戻る
- 朝に入力を促さない。座って開いたときに直せば足りる

### 拘束時間の扱い
- 家庭教師などで時間が潰れる日は、**箱数を減らすだけ**で表現する
- `−` を押したとき、理由ラベルを**任意で**付けられる（未入力のまま進められること。必須にしない）
- 目的は「今月、家庭教師で何箱使ったか」を後から見られるようにすること

### 使い切ったら
- 箱を使い切ったら**今日は終了**。明示的にそう表示する
- 箱に入りきらなかったやりたいことは**翌日に回す**（既存の「今日はやらない」と同じ扱い）

---

## やりたいこと

- **登録時に必要な箱数をセットで入力する。** 箱数はやりたいことに固定で持たせる
  - 例：「過去問 = 2箱」「30分歩く = 1箱」
- 2箱のやりたいことは連続した2箱を占める
- 既存の頻度（単発／毎日／週N回／月N回／N日にM回）はそのまま維持する
- 既存のリンク（やりたいことの名前タップで開く）もそのまま維持する
- **実績の記録欄を作る。** 実際に何箱かかったかを入力できる。任意入力で、飛ばせること

### 1日の流れ
```
箱数を調整する
   ↓
やりたいことを箱に入れる
   ↓
箱が埋まったら、残りのやりたいことは翌日に回す
   ↓
今日のやりたいことをやり切る
   ↓
今日は終了
```

---

## ルーティーン（チェックリスト）

- **箱を消費しない。** やりたいこととは別枠
- **午前用・午後用の2種類**だけ用意する
- いまの時間帯に該当する方だけを画面に出す
- 完了ボタンで完了。日付が変わればリセットされる

---

## 画面（PC前提）

- **1画面で完結させる。** タブを行き来させない
  - 左：いま、これ ＋ 今日の箱の列
  - 右：まだ箱に入れていないやりたいこと
  - 時間帯に応じたルーティーン
- **残り箱数を常に見えるところに置く**（他のことをやりすぎない抑止として機能させる）
- **キーボードで操作できるようにする**（完了、次へ、箱数の増減）
- タブを開きっぱなしにしても、午前2時をまたいだら表示が切り替わること

---

## 引き継ぐもの / 捨てるもの

| | |
|---|---|
| 引き継ぐ | 頻度つきのやりたいこと、記録の取り消し、リンク付きのやりたいこと、人生哲学、バックアップ書き出し／読み込み |
| 置き換える | 朝／夜 → 午前／午後（区切りも変更） |
| 置き換える | ホーム画面のレイアウト（縦積み → 1画面） |
| 捨てる | （なし） |

---

## まだ決めていないこと

1. やりたいことを箱に入れる操作（ドラッグするのか、クリックで次の空き箱に入るのか）
2. 実績（かかった箱数）を溜めたあと、どう見せるか
3. 箱を使い切ったあと、それでも何かやった場合の記録方法
4. 平日／休日の判定を曜日で自動にするか、手動で切り替えるか

---

## 作業の順番

1. **仕様の確定**（このファイル） ← いまここ
2. レイアウトの刷新のみ（機能追加はしない）
3. 箱モデルの実装

---
---

# 追加仕様：1週間の予定（2026-08-11 決定）

ここから下は 2026-08-11 に本人と詰めて**決定した**もの。まだ実装していない。
上の「まだ決めていないこと」4点は `HANDOFF.md` で既に決着ずみ。

---

## 1週間ページ

- **今日を起点に7日分**の箱を、1つの画面で確認・編集できる
- やりたいことリストから**ドラッグして箱に入れる**（今日タブと同じ操作）
- **今日タブは今までどおり。** 設定した箱数が出て、そこで編集もできる
- 各日に**仕事／休み**を入力できる（今日タブの「平日／休み」を、他の日にも広げるだけ）

---

## 達成の数え方：行動ベース

**達成は「その日に達成ボタンを押したとき」に数える。** 箱に置いただけでは達成にならない。

リストに出すかどうかは、次の式で決める。

```
残り ＝ 回数 － その期間にやった数 － これから来る箱に置いてある数

残り > 0 のときだけ、やりたいことリストに出す
```

### この式から自動的に決まること

- **置ききったらリストから消える**（「入れきったら、もう終わり！」）
- **サボった分は勝手に戻ってくる。**
  過ぎた日の未実行の箱は「これから来る箱」に入らないので、`残り` が増えて再びリストに出る。
  置き直せばよい。放置すれば週末に溜まって見えるので、抑止としても働く
- **「月に何回」も同じ式で解ける。** 今月の残りをそのまま出すだけ。
  1週間に何個置くかは本人が決める。特別なルールは要らない

> **要確認（実装前に決める）。**
> `periodOf()` の「今週」は**月曜はじまり**だが、1週間ページは**今日起点の7日**。
> 木曜に開くと画面には木〜水が並び、**後半の月・火は「来週ぶん」**になる。
> 画面上でどちらの週のぶんかを区別して見せる必要がある。

---

## 単発の回数

- 単発のやりたいことに**総回数**を持たせられる（**期限なし**。「全部で5回」「5周する」）
- 回数ぶんやり切ったら、やりたいことリストから消えて**「達成したものリスト」**に入る
- いまの `done: true/false` を、回数と達成数に置き換える

---

## 箱と時刻

**箱はこれまでどおり順番だけを持つ。時刻は持たない。**
時刻は「**何番目の箱に置くか**」を決めるためと、本人が見当をつけるために使うだけ。

- 平日／休日それぞれに、**1箱目の開始時刻**を設定する
- 1箱 = 30分なので、`箱N の開始 ＝ 開始時刻 ＋ 30分 ×(N−1)`
- 目立つ時刻（18時・22時など）に**線を引いて**見せる。全部の箱には出さない

例：平日8箱・18:00 開始

| 箱 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 目安 | 18:00 | 18:30 | 19:00 | 19:30 | 20:00 | 20:30 | 21:00 | 21:30 |

→ 8箱ちょうどで 22:00 に終わる。

> **あくまで目安。** 休憩を挟めば実時間はずれていく。本人確認ずみ。

---

## 曜日・時刻つきの登録と、自動配置

- **毎週決まった予定**は、曜日＋時刻を一緒に登録できる。**毎日のもの**も同様
- 時刻は**何番目の箱から置き始めるか**を決めるために使う。
  複数箱のものは、**そこを開始位置**として続きの箱を占める
  - 例：「毎週火曜19時・2箱」→ 平日18:00開始なら**箱3から箱4**

### 自動配置のルール（重要）

- **自動で入るのは、1週間分の予定で「その日が作られたとき」の1回だけ。**
- **それ以降、アプリは箱に手を出さない。** 移動も削除も**ユーザーの自由**
- 一度置いたものを本人が外しても、**アプリが置き直すことはない**
- 作られた時点で**衝突が起きそうなら**（箱が埋まっている／箱が足りない）、
  どこかのタイミングで**その旨をアラートして、ユーザーに変更を求める**。
  **アプリが勝手にずらしたり消したりしない**

---

## 今週だけの抜け道

決まった回数のものが、**その時だけ**増えたり減ったりすることへの逃げ道。

- 1週間ページで、その項目の回数を**今週だけ**増減できる（週2回 → 今週だけ3回／1回）
- **週キー**（`2026-W33` など）で持つ。**週が変われば自動で元の回数に戻る**
- 「固定の予定を今週の火曜だけ外す」も同じ枠で扱う

---

## 遠い日程

- 「来年やりたい」のような**遠い予定**も登録できる
- **リマインドの日程**を決めておく
- **日が近づくまでは別の場所に隠れている。** ふだんのやりたいことリストには出さない

---

## やりたいことの編集

- 登録した後から**編集できる**ようにする（曜日・時刻・回数・箱数など）
- 他の項目がどれも「後から直せること」を前提にしているので、**これが全部の前提**になる

---

## 実装の順番

壊れたときに原因を追えるよう、この順で進める。

| | やること | 備考 |
|---|---|---|
| 1 | **やりたいことの編集機能** | 他の全部の前提 |
| 2 | **単発の回数＋達成したものリスト** | 独立していて、いちばん切り離しやすい |
| 3 | **1週間ページ**（表示と仕事／休みまで） | ドラッグはまだ入れない |
| 4 | **週へのドラッグ配置＋回数の数え方の変更** | ここが本丸。上の「要確認」を先に片付ける |
| 5 | 箱の時刻の線 | |
| 6 | 曜日・時刻つきの登録と自動配置 | |
| 7 | 遠い日程とリマインド | |

### 実装上そろえること

- データ版は **v6 → v7**。`migrate()` を足す
- 文言を変えたら**版を4か所そろえる**（`HANDOFF.md`「バージョンと更新」）
- 画面に出す言葉は「**やりたいこと**」。「タスク」とは書かない
