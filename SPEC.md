# 自己管理アプリ 仕様（箱モデル）

2026-08-09 時点で確定した仕様。実装前の合意事項として残す。

---

## 前提

- ~~**PCで使う。** スマホ対応は考えない~~
  → **2026-08-14 に一部取り消し。** 昼はスマホ、夜はPC。下の
  「追加仕様：端末をまたいで使う」を読むこと
- 自由時間 ＝ PCを触れる時間。「このアプリだけ見ておけばいい」状態を作るのが目的
- 大きな予定（家庭教師など）の詳細は外部で管理する。このアプリは**消費した時間の量だけ**を知る

---

## 1日の区切り

| 区分 | 時間 |
|---|---|
| 日付の切り替え | **午前2時** |
| 午前 | 2:00 – 14:00 |
| 午後 | 14:00 – 翌2:00 |

- 既存の「朝(4-12) / 夜(18-4)」は廃止し、この午前／午後に置き換える
- 深夜1時はまだ「前日の午後」。日付も箱数の調整もリセットされない

---

## 箱

### 定義
- **1箱 = 30分**。箱は時間そのもの
- **1箱に入るやりたいことは1つだけ**。小さい用事は「細々した用事」のようなやりたいことを1つ作ってまとめる
- 箱は**個数と順番だけ**を持つ。時刻は持たない（9:00-9:30 のような割り付けはしない）

### 箱数の決め方
- **基本値**を設定しておく（平日◯箱／休日◯箱）
- その日の箱数は基本値から自動で始まる
- `＋` `−` ボタンでその日だけ増減する。翌日は基本値に戻る
- 朝に入力を促さない。座って開いたときに直せば足りる

### 拘束時間の扱い
- 家庭教師などで時間が潰れる日は、**箱数を減らすだけ**で表現する
- `−` を押したとき、理由ラベルを**任意で**付けられる（未入力のまま進められること。必須にしない）
- 目的は「今月、家庭教師で何箱使ったか」を後から見られるようにすること

### 使い切ったら
- 箱を使い切ったら**今日は終了**。明示的にそう表示する
- 箱に入りきらなかったやりたいことは**翌日に回す**（既存の「今日はやらない」と同じ扱い）

---

## やりたいこと

- **登録時に必要な箱数をセットで入力する。** 箱数はやりたいことに固定で持たせる
  - 例：「過去問 = 2箱」「30分歩く = 1箱」
- 2箱のやりたいことは連続した2箱を占める
- 既存の頻度（単発／毎日／週N回／月N回／N日にM回）はそのまま維持する
- 既存のリンク（やりたいことの名前タップで開く）もそのまま維持する
- **実績の記録欄を作る。** 実際に何箱かかったかを入力できる。任意入力で、飛ばせること

### 1日の流れ
```
箱数を調整する
   ↓
やりたいことを箱に入れる
   ↓
箱が埋まったら、残りのやりたいことは翌日に回す
   ↓
今日のやりたいことをやり切る
   ↓
今日は終了
```

---

## ルーティーン（チェックリスト）

- **箱を消費しない。** やりたいこととは別枠
- **午前用・午後用の2種類**だけ用意する
- いまの時間帯に該当する方だけを画面に出す
- 完了ボタンで完了。日付が変わればリセットされる

---

## 画面（PC前提）

- **1画面で完結させる。** タブを行き来させない
  - 左：いま、これ ＋ 今日の箱の列
  - 右：まだ箱に入れていないやりたいこと
  - 時間帯に応じたルーティーン
- **残り箱数を常に見えるところに置く**（他のことをやりすぎない抑止として機能させる）
  → 2026-08-15：画面上部の帯はやめ、**「今日の箱」の見出しの中**に移した（`HANDOFF.md`「画面」）
- **キーボードで操作できるようにする**（完了、次へ、箱数の増減）
- タブを開きっぱなしにしても、午前2時をまたいだら表示が切り替わること

---

## 引き継ぐもの / 捨てるもの

| | |
|---|---|
| 引き継ぐ | 頻度つきのやりたいこと、記録の取り消し、リンク付きのやりたいこと、人生哲学、バックアップ書き出し／読み込み |
| 置き換える | 朝／夜 → 午前／午後（区切りも変更） |
| 置き換える | ホーム画面のレイアウト（縦積み → 1画面） |
| 捨てる | （なし） |

---

## まだ決めていないこと

1. やりたいことを箱に入れる操作（ドラッグするのか、クリックで次の空き箱に入るのか）
2. 実績（かかった箱数）を溜めたあと、どう見せるか
3. 箱を使い切ったあと、それでも何かやった場合の記録方法
4. 平日／休日の判定を曜日で自動にするか、手動で切り替えるか

---

## 作業の順番

1. **仕様の確定**（このファイル） ← いまここ
2. レイアウトの刷新のみ（機能追加はしない）
3. 箱モデルの実装

---
---

# 追加仕様：1週間の予定（2026-08-11 決定）

ここから下は 2026-08-11 に本人と詰めて**決定した**もの。
**7項目すべて実装ずみ**（版 `2026-08-11.2`、データ v7）。いまの実装の詳細は `HANDOFF.md`。
上の「まだ決めていないこと」4点は `HANDOFF.md` で既に決着ずみ。

---

## 1週間ページ

- **週は日曜はじまり。** 日曜〜土曜の7日分の箱を、1つの画面で確認・編集できる
- **来週ぶんも設定できる。** 今週と来週を行き来できるようにする
- やりたいことリストから**ドラッグして箱に入れる**（今日タブと同じ操作）
- **今日タブは今までどおり。** 設定した箱数が出て、そこで編集もできる
- 各日に**仕事／休み**を入力できる（今日タブの「平日／休み」を、他の日にも広げるだけ）

> **決定：切り替え。** 今週と来週はタブで切り替える。2週間は並べない。

---

## 達成の数え方：行動ベース

**達成は「その日に達成ボタンを押したとき」に数える。** 箱に置いただけでは達成にならない。

リストに出すかどうかは、次の式で決める。

```
残り ＝ 回数 － その期間にやった数 － これから来る箱に置いてある数

残り > 0 のときだけ、やりたいことリストに出す
```

### この式から自動的に決まること

- **置ききったらリストから消える**（「入れきったら、もう終わり！」）
- **サボった分は勝手に戻ってくる。**
  過ぎた日の未実行の箱は「これから来る箱」に入らないので、`残り` が増えて再びリストに出る。
  置き直せばよい。放置すれば週末に溜まって見えるので、抑止としても働く
- **「月に何回」も同じ式で解ける。** 今月の残りをそのまま出すだけ。
  1週間に何個置くかは本人が決める。特別なルールは要らない

### 週の起点：日曜はじまり

**アプリ全体で、週は日曜はじまりにそろえる。** 1週間ページも「週に何回」の集計も同じ。
起点がそろうので、**ページに並ぶ7日と「今週ぶん」が常に一致する。**

- 週の起点を決めているのは **`periodOf()` の1か所だけ**。他に曜日に依存した処理はない（確認ずみ）
  - いま　`d.setDate(d.getDate() - ((d.getDay() + 6) % 7))`  ← 月曜はじまり
  - あと　`d.setDate(d.getDate() - d.getDay())`  ← 日曜はじまり
- 週の区切りも**午前2時**に合わせる（既存のまま）

> **直した週にだけ、集計が1回ずれる。** 記録（`t.log`）は消えないが、
> 「今週やった数」に**直前の日曜ぶんが入ってくる**。実害はないが、
> 直後に「あれ、1回増えた」と見えることがある。

---

## 単発の回数

- 単発のやりたいことに**総回数**を持たせられる（**期限なし**。「全部で5回」「5周する」）
- 回数ぶんやり切ったら、やりたいことリストから消えて**「達成したものリスト」**に入る
- いまの `done: true/false` を、回数と達成数に置き換える

---

## 箱と時刻

**箱はこれまでどおり順番だけを持つ。時刻は持たない。**
時刻は「**何番目の箱に置くか**」を決めるためと、本人が見当をつけるために使うだけ。

- 平日／休日それぞれに、**1箱目の開始時刻**を設定する
- 1箱 = 30分なので、`箱N の開始 ＝ 開始時刻 ＋ 30分 ×(N−1)`
- 目立つ時刻（18時・22時など）に**線を引いて**見せる。全部の箱には出さない

例：平日8箱・18:00 開始

| 箱 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 目安 | 18:00 | 18:30 | 19:00 | 19:30 | 20:00 | 20:30 | 21:00 | 21:30 |

→ 8箱ちょうどで 22:00 に終わる。

> **あくまで目安。** 休憩を挟めば実時間はずれていく。本人確認ずみ。

---

## 曜日・時刻つきの登録と、自動配置

- **毎週決まった予定**は、曜日＋時刻を一緒に登録できる。**毎日のもの**も同様
- 時刻は**何番目の箱から置き始めるか**を決めるために使う。
  複数箱のものは、**そこを開始位置**として続きの箱を占める
  - 例：「毎週火曜19時・2箱」→ 平日18:00開始なら**箱3から箱4**

### 自動配置のルール（重要）

- **自動で入るのは、1週間分の予定で「その日が作られたとき」の1回だけ。**
- **それ以降、アプリは箱に手を出さない。** 移動も削除も**ユーザーの自由**
- 一度置いたものを本人が外しても、**アプリが置き直すことはない**
- 作られた時点で**衝突が起きそうなら**（箱が埋まっている／箱が足りない）、
  どこかのタイミングで**その旨をアラートして、ユーザーに変更を求める**。
  **アプリが勝手にずらしたり消したりしない**
- **来週ぶんを先に開くと、その時点で来週の日が作られて自動配置が走る。**
  そのあとで仕事／休みを変えても、固定の予定を編集しても、**もう置き直されない**。
  ここが衝突アラートのいちばんの出しどころになる

---

## 今週だけの抜け道

決まった回数のものが、**その時だけ**増えたり減ったりすることへの逃げ道。

- 1週間ページで、その項目の回数を**今週だけ**増減できる（週2回 → 今週だけ3回／1回）
- **週キー**（`2026-W33` など）で持つ。**週が変われば自動で元の回数に戻る**
- 「固定の予定を今週の火曜だけ外す」も同じ枠で扱う

---

## 遠い日程

- 「来年やりたい」のような**遠い予定**も登録できる
- **リマインドの日程**を決めておく
- **日が近づくまでは別の場所に隠れている。** ふだんのやりたいことリストには出さない

---

## やりたいことの編集

- 登録した後から**編集できる**ようにする（曜日・時刻・回数・箱数など）
- 他の項目がどれも「後から直せること」を前提にしているので、**これが全部の前提**になる

---

## 実装の順番

壊れたときに原因を追えるよう、この順で進める。

| | やること | 備考 |
|---|---|---|
| 1 | **やりたいことの編集機能** | 他の全部の前提 |
| 2 | **単発の回数＋達成したものリスト** | 独立していて、いちばん切り離しやすい |
| 3 | **1週間ページ**（表示・仕事／休み・来週ぶんまで） | ここで `periodOf()` を日曜はじまりに直す。ドラッグはまだ入れない |
| 4 | **週へのドラッグ配置＋回数の数え方の変更** | ここが本丸 |
| 5 | 箱の時刻の線 | |
| 6 | 曜日・時刻つきの登録と自動配置 | |
| 7 | 遠い日程とリマインド | |

### 実装上そろえること

- データ版は **v6 → v7**。`migrate()` を足す
- 文言を変えたら**版を4か所そろえる**（`HANDOFF.md`「バージョンと更新」）
- 画面に出す言葉は「**やりたいこと**」。「タスク」とは書かない

---
---

# 追加仕様：端末をまたいで使う（2026-08-14 決定）

ここから下は 2026-08-14 に本人と詰めて**決定した**もの。**実装ずみ**（版 `2026-08-14.3`）。
手順は `sync/README.md`、実装の詳細は `HANDOFF.md`「同期」。

---

## ふだんの流れ

```
昼　スマホで、今日の箱・1週間の箱にやりたいことを入れる
 ↓
夜　パソコンを開いて、それを見ながら実行する
```

**リアルタイムでなくてよい。** 開いたときに合っていれば足りる。

---

## 前提の変更：スマホでも使う

上の「前提」の **「PCで使う。スマホ対応は考えない」を、ここで一部取り消す**（本人の指示）。

- **スマホで使うのは「今日」タブと「1週間」タブ、およびそこに関係するものだけ。**
  やりたいことの登録・哲学・設定はPCのまま。ここを広げない
- 箱に入れる操作（ドラッグ）は、**本人のスマホで既に動いている**ので変えない

> **コードにはタップで箱に入れる道が無い。** `#unplaced` にあるのは `dragstart` だけで、
> クリックで入れる処理は書いていない（`renderNow()` の「クリックで入ります」は古い文言）。
> いま動いているのは、ブラウザが長押しをドラッグとして扱っているため。
> **別の端末で入れられなかったときは、これが理由。** そのときタップの道を足す。

---

## 同期するもの / しないもの

| | 中身 | なぜ |
|---|---|---|
| **同期する** | 日記 ／ 箱（`days`）／ やりたいこと（`tasks`）／ ルーティーンと実施記録 ／ 基本の箱数・開始時刻 ／ 今週だけの回数 ／ 哲学・一言 | 昼にスマホで入れたものを、夜にPCで見るために要る |
| **端末ごとに持つ** | **画像** ／ **タイマーの音** ／ **ブラウザの場所** | 画像と音は重い（1つ400KB）。ブラウザの場所は Windows のパスなので、スマホでは意味がない |

> **「箱だけ」は取り出せない。** 箱はやりたいことのIDを持っているだけなので、
> やりたいことを一緒に送らないと、向こうで中身が読めない。

日記は、スマホで読み書きしなくても**同期する**（消えないための保険。もともとの動機がこれ）。

---

## 突き合わせ方

**塊ごとに新旧を比べない。1件ごとに比べる。**

| 対象 | 規則 |
|---|---|
| 日記 | **1件ごと**に、あとに書いた方を採る（2026-08-15 以降は同じ日に何件でも置ける） |
| 箱（`days`） | **その日ごと**に、あとに触った方を採る |
| やりたいこと（`tasks`） | **1件ごと**に、あとに直した方を採る |
| やった記録（`log`） | **両方を足し合わせる**（後勝ちにしない） |

`log` だけ足し算にするのは、昼にスマホで押したぶんと夜にPCで押したぶんが、
**どちらも残らないといけない**ため。ここを後勝ちにすると、押した記録が消える。

昼はスマホ・夜はPCと時間が分かれているので、同じものを同時にいじる場面はほぼ来ない。
来たときだけ後勝ちになる（`log` を除く）。**それで構わないことを本人が確認ずみ**（2026-08-14）。

### 各データに「いつ触ったか」を持たせる → **実装で変えた**

> **触った時刻は持たせなかった。** 書き換えるところ全部に足すと、
> **1か所でも漏らした瞬間に、そこだけ同期されなくなる**（漏れても気づけない）。
> 代わりに「前回合わせたときの中身の印」を控えて、印が変わっていたら直したとみなす。
> **`save()` を通れば必ず拾えるので、漏れようがない。** 詳細は `HANDOFF.md`「同期」。

---

## 置き場

```
各端末の localStorage（これまでどおり正）
      ↕ 合言葉つきの fetch
Vercel の Function（鍵を隠す入口）
      ↕ service_role キー（Function の環境変数）
Supabase
```

- **localStorage を正のまま残す。** ネットが無くても今までどおり書ける
- **Supabase を直に叩かない。** anon key が公開ソースに載るため
- **鍵を隠すだけでは足りない。** 無認証の Function は URL を知っていれば誰でも読める。
  合言葉をヘッダに載せて入口を閉じる（初回だけ入力、以後 localStorage）

---

## 暗号化と、人の分け方（2026-08-14 決定）

**使うのは本人だけではない。** もう1人（`Haru`）が既に使っている。

| 論点 | 決定 |
|---|---|
| 人の分け方 | **名前 ＋ 合言葉。** `niku` と `Haru`。それぞれの引き出しは完全に分かれる |
| 合言葉 | **各自が決める。** 管理者にも分からない（渡すのは変換後の「入口用の値」だけ） |
| 暗号化 | **最初からやる。** あとからだと、平文の行が残って移行が要るため |
| 何を隠すか | **中身（`body`）を丸ごと。** 文字も数字も区別しない |
| 見出し（日付・ID） | **ハッシュにして伏せる。** 「いつ日記を書いたか」も管理者に見せない |
| 人を増やす | **できる。** 環境変数に1組足して再デプロイするだけ。5人を超えたら表に移す |

### 合言葉は1つ。端末の中で3つに分ける

```
合言葉（端末から出ない）
  ├→ 入口用（auth）  … サーバーに送る。「この人だ」の確認だけ
  ├→ 中身の鍵（enc） … 送らない。暗号化と復号
  └→ 見出しの鍵（id）… 送らない。日付やIDを伏せる
```

`auth` は元を一方向に潰した値なので、**これを渡しても `enc` は作れない。**
だから「入口用の値を管理者に渡しても、中身は読まれない」が成り立つ。

- 作り方（塩・回数）は **`sync/key.html` と `app.js` の2か所に同じものがある。
  片方だけ直すと、これまでのデータが開けなくなる**
- 塩に**名前を混ぜる**。同じ合言葉を2人が選んでも、別の鍵になる

### なぜ全部隠してよいのか

**サーバー側の突き合わせは `at` の大小しか見ていない。**
中身を読む必要がどこにも無いので、丸ごと隠しても仕組みは変わらない。
`log` の足し算だけは中身を見るが、それは**端末側で開いてから**やる。

### 代償（3つとも、実装で手当てすること）

| | 中身 | 手当て |
|---|---|---|
| **忘れたら終わり** | 鍵を誰も預かっていないので、合言葉を無くすと永久に開けない | バックアップ書き出し（平文JSON）を促し続ける |
| **端末ごとに入力が要る** | PCとスマホで同じ合言葉を入れる | — |
| **打ち間違いが静かに壊す** | 違う合言葉だと「開けないだけ」になり、壊れて見える | **確認用の1行を置き、開けなければその場で「合言葉が違います」と出して止める** |

---

## 落とし穴（実装前から分かっているもの）

### 1. 消したものが生き返る

スマホで消したやりたいことを、PCがまだ持っていれば、次にPCが送った時点で復活する。
**「消した」ことも記録して送る**（墓標）。日記の削除も同じ。

### 2. 自動配置が二重に走る

「決まった時間」を持つものは、**その日が作られたときの1回だけ**箱に入る決まり（`ensureDay()`）。
昼にスマホが来週の日を作ると、夜にPCが同じ日をもう一度作って置き直してしまう。

→ **起動時は、その日を作る前に取ってくる。** いまの起動は
`prune(); rollDay(); renderAll();` の順で、`rollDay()` がその場で日を作っている。
ここに取得を挟む順番を決めること。

---

## 作業の順番

| | やること | 備考 |
|---|---|---|
| 1 | ~~GitHub Pages に寄せる~~ | 済。 https://nikunokuni.github.io/iamidol/ |
| 2 | ~~Vercel に Function を1本、Supabase にテーブルを1つ~~ | **書いた**（`api/sync.js` / `sync/schema.sql` / `sync/key.html`）。**通るかの確認はこれから**。手順は `sync/README.md` |
| 3 | ~~各データに「いつ触ったか」を足す~~ | **やめた**（上記。中身の印で見る） |
| 4 | ~~合言葉の入力と、3つの鍵を作るところ~~ | 済 |
| 5 | ~~取得 → 復号 → 突き合わせ → 暗号化 → 送信~~ | 済 |
| 6 | ~~墓標と、合言葉の打ち間違いの検知~~ | 済 |
| 7 | ~~設定タブに同期の状態~~ | 済。**残るは実機での確認だけ** |

**1 を先に潰すこと。** ここが通らないと、下の設計ごと変わる。
