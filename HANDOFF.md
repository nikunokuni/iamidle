# 引き継ぎメモ

2026-08-09 作成 / 2026-08-09 更新。次のセッションはこれを最初に読むこと。

---

## このプロジェクト

個人用の自己管理アプリ。**単一のHTMLファイル**（`index.html`、1,177行）。
ライブラリなし、ビルドなし、サーバー不要。ダブルクリックで開けば動く。

**PC専用**として作る。スマホ対応は考えない。
理由：本人の自由時間＝PCを触れる時間であり、「このアプリだけ見ておけばいい」状態を作って他のことに逸れるのを防ぐのが目的。

---

## 開発の進め方

### GitHubのファイルが唯一の正

**このリポジトリ（`nikunokuni/iamidol`）にあるファイルが常に最新かつ正。**
手元のコピーやプレビュー環境の状態を正としないこと。作業のはじめに必ずリポジトリの現物を読むこと。

### 流れ

1. Claude が作業ブランチを切って実装し、そのブランチに push する
2. 本人が GitHub 上で中身を確認してマージする
3. マージされたものが次の正になる

- **`main` への直接 push は、本人からその都度はっきり頼まれたときだけ。**
  それ以外は必ず作業ブランチに push する。自分の判断で `main` に入れない。
- **`--force` push をしない。** 履歴を書き換える必要が出たら、まず本人に相談する。
- 本人は原則、手動アップロードをしない（Claude に任せる）。
  もし何らかの事情で手動アップロードした場合は、その旨を Claude に伝えること。

### 環境について

セッションによって使える道具が違う（`gh` CLI が無い、GitHub API 経由でしか操作できない等）。
**作業の最初に、git と GitHub 操作が実際に通るかを確認してから進めること。** 前提を置かない。

---

## 最優先でやること

### 1. レイアウトの刷新（機能追加はしない）

いまはスマホ想定で、タブを縦に積む構成。PC前提で1画面に組み直す。
詳細は `SPEC.md` の「画面（PC前提）」。

**この刷新のタイミングで、`index.html` / `style.css` / `app.js` の3ファイルに分割する（本人承認ずみ）。**
分割してもビルドは不要で、ローカルでもGitHub Pagesでも同じように動く。
ただし **`type="module"` は使わないこと**（`file://` でCORSに引っかかって動かなくなる）。

**機能は増やさない。** 見た目とファイル構成だけを変える。

### 2. 箱モデルの実装

`SPEC.md` に確定仕様がある。**仕様は本人と合意済み。勝手に変えないこと。**

---

## いまある機能

- **タスク**：頻度つき（単発／毎日／週N回／月N回／N日にM回）
- **進捗とペース判定**：月10回なら「今日の時点で何回済んでいるべきか」を計算し、遅れているものだけホームに出す
- **朝／夜**：時間帯でタスクを振り分け、いまの時間帯のものを上位に出す（→ 仕様変更で午前／午後に置き換える）
- **今日はやらない**：完了でも未達成でもない見送り。翌日また出る
- **リンク付きタスク**：タスク名クリックで登録したURLを開く（例：筋トレ → 記録サイト）
- **取り消し**：削除・回数記録・完了のすべてに取り消し手段がある
- **人生哲学**：画面上部に常時1つ表示。タップで次へ。日付が変わると自動で進む
- **バックアップ**：JSONの書き出し／読み込み

---

## データ

- **localStorage**、キーは `jiko-kanri-v1`。値はJSON1つ
- オリジン単位で保存される。**ローカル（file://）とGitHub Pagesは別々の引き出し**で、同期しない
- **常に最新の状態が保存されていればよい。過去のデータに戻す要件は無い**（本人確認ずみ）
- コードのgit巻き戻しとデータは無関係。コードを戻してもタスクは消えない

> **本人の実データが入っている。** テストで壊さないこと。下の「検証のやり方」を必ず読むこと。

---

## 開発上のハマりどころ（実際に踏んだもの）

### `confirm()` / `prompt()` が効かない
プレビュー環境ではネイティブダイアログが無効化されていて、**呼ぶと即座に `false` を返す**。
そのせいで削除ボタンが無反応になる事故が起きた。
→ アプリ内モーダル（`openModal` / `askConfirm`）を実装ずみ。**今後もネイティブダイアログは使わないこと。**

### 検証で本人のデータを壊さない
過去に2回、テストデータで実データを上書きしかけた。安全なやり方：

```js
// localStorage に書かずに描画だけ検証する
const real = S;                 // 実データを退避
S = { /* 合成データ */ };        // 差し替え（save() は呼ばない）
renderNow(); renderToday();     // 描画を確認
S = real; renderAll();          // 元に戻す
```

- `save()` を呼ばなければ localStorage は変わらない
- 関数単体（`todayNeed`, `statOf` など）は合成オブジェクトを渡せばそのまま検証できる
- `nowSlot` のような時刻依存の関数は一時的に差し替えて、朝／昼／夜の挙動を確認できる

### 本人がプレビューを同時に操作していることがある
ページのメモリ上の状態と localStorage がずれる。`location.reload()` の前には `save()` を呼ぶこと。

### その他
- **`file://` で動くこと**が必須。`type="module"` は使わない（CORSで動かなくなる）
- ページ内スクリプトが `const S` をグローバルに宣言している。評価用スクリプトで `const S` を再宣言すると `SyntaxError` になるので、別名を使うか即時関数で囲む

---

## 既知の小さなバグ（未修正・保留）

レイアウト刷新と箱モデルが先。**いま直さなくてよい**が、該当箇所を触るついでがあれば直す。
行番号は分割前の `index.html` のもの。

| 箇所 | 内容 |
|---|---|
| `1091` vs `967` | 設定画面の「遅れ」件数が一覧と食い違う。集計側だけ毎日タスクを除外していないため、毎日タスクが今日未実行なだけで遅れに数えられる |
| `747` | `doneAt` を持たない古い単発タスクは、完了しても「今日やったこと」に出ない（`undefined >= from` が常に false）。`999` はチェックを外すときにも `doneAt` を書いていて不整合 |
| `1162`, `1173` | タブを開きっぱなしだと日付をまたいでも一覧が再計算されない（時計だけ更新される）。`SPEC.md` の「午前2時をまたいだら表示が切り替わること」に直結 |
| `389` | `DEFAULTS.links` がどこからも使われていない。`statOf().todayDone` も未使用 |
| `577` | `renderPhiloBar` の `philoIdx` はみ出し補正が `save()` されない |

---

## git の状態

```
d3b3c2b Add files via upload   ← HANDOFF.md, SPEC.md
f69de2d Delete README.md
796bb90 Create README.md       ← 中身を取り違えた事故。削除ずみ。無視してよい
7bc475c Add files via upload   ← index.html（箱モデル着手前の現状）
```

- リモート：`nikunokuni/iamidol`（接続ずみ）。既定ブランチは `main`
- ここまでの履歴はすべてブラウザからの手動アップロード。**この内容が正**
- README は現状置いていない。必要になったら作る

---

## ファイル

| ファイル | 中身 |
|---|---|
| `index.html` | アプリ本体。これ1つで動く（レイアウト刷新で3分割予定） |
| `SPEC.md` | 箱モデルの確定仕様。実装前に必ず読む |
| `HANDOFF.md` | このファイル |

---

## まだ決めていないこと

`SPEC.md` の「まだ決めていないこと」（箱への入れ方、実績の見せ方など）を参照。
レイアウト刷新の段階では踏み込まなくてよい。

---

## 相手について

- 仕様を自分で詰めるタイプ。メリットとデメリットを自分で言語化してから相談してくる
- 判断は本人がする。選択肢と推奨を出せば決められる。**勝手に仕様を広げない**
- 「まだ実装しない」と言われたら、本当に実装しないこと
