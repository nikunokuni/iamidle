# 引き継ぎメモ

2026-08-09 作成 / 最終更新 2026-08-16（**日記に年月のしぼりこみ**・**今日タブを2カラム固定枠に作り直し**）。
次のセッションはこれを最初に読むこと。

---

## このプロジェクト

個人用の自己管理アプリ。**3ファイル構成**（`index.html` / `style.css` / `app.js`）。
ライブラリなし、ビルドなし、サーバー不要。`index.html` をダブルクリックで開けば動く。

もとは**PC専用**として作った（自由時間＝PCを触れる時間であり、「このアプリだけ見ておけばいい」
状態を作って他のことに逸れるのを防ぐのが目的）。

> **2026-08-14 に一部変更。** 昼はスマホで箱に入れ、夜にPCで実行する流れにする、と本人が決めた。
> **スマホで使うのは「今日」タブと「1週間」タブだけ**で、登録・哲学・設定はPCのまま。
> **同期を実装した**（下の「同期」）。使っているのは本人（`niku`）と、もう1人（`Haru`）の2人。
> 3ファイル構成は変わらないが、**サーバー側に `api/sync.js` と `sync/` が増えた**。

### 言葉づかい

**画面に出す言葉は「やりたいこと」。「タスク」とは書かない。**
2026-08-11 に本人の指示で言い換えた。コメントやこのメモの中でも「やりたいこと」で通す。

- 単発のやりたいこと／くり返しのやりたいこと／リンク付きのやりたいこと
- 下部タブの名前も「やりたいこと」

ただし**コードの中の英語の名前（`tasks` `taskId` `renderTasks` `sec-tasks` など）はそのまま**。
`S.tasks` は localStorage に保存されている形そのものなので、触ると既存のデータが読めなくなる。

---

## 開発の進め方

### GitHubのファイルが唯一の正

**このリポジトリ（`nikunokuni/iamidle`）にあるファイルが常に最新かつ正。**
手元のコピーやプレビュー環境の状態を正としないこと。作業のはじめに必ずリポジトリの現物を読むこと。

### 流れ

1. Claude が作業ブランチを切って実装し、そのブランチに push する
2. 本人が GitHub 上で中身を確認してマージする
3. マージされたものが次の正になる

- **`main` への直接 push は、本人からその都度はっきり頼まれたときだけ。**
  それ以外は必ず作業ブランチに push する。自分の判断で `main` に入れない。
- **`--force` push をしない。** 履歴を書き換える必要が出たら、まず本人に相談する。
- 本人は原則、手動アップロードをしない（Claude に任せる）。
  もし何らかの事情で手動アップロードした場合は、その旨を Claude に伝えること。

### 環境について

セッションによって使える道具が違う（`gh` CLI が無い、GitHub API 経由でしか操作できない等）。
**作業の最初に、git と GitHub 操作が実際に通るかを確認してから進めること。** 前提を置かない。

---

## いまの状態

`SPEC.md` の「作業の順番」は 3 まで終わっている。

1. ~~仕様の確定~~ 済
2. ~~レイアウトの刷新（3ファイル分割を含む）~~ 済
3. ~~箱モデルの実装~~ 済

そのあと、本人の依頼で **「1つのやりたいことに複数のリンク＋PCのアプリ」** を足した（下の「リンクとアプリ」）。
`SPEC.md` は「既存のリンクはそのまま維持する」としか書いていないので、**本体は書き換えていない。**

### リンクとアプリの件は、いったん保留

本人の指示で **2026-08-09 に保留**。動くところまで行ったので、ここから先は触らない。
次は別の機能に移る。

**本人の実機（Windows）で確認できたこと：**

- 起動ファイル(.bat)から OBS・VTube Studio・ブラウザがまとめて起動する
- 「Chromeで開く」「Braveで開く」の指定が、指定どおりのブラウザで開く
- `.bat` のショートカットにホットキーを割り当てて、キー一発で起動できる
  （`.lnk` は**デスクトップかスタートメニューに置かないと効かない**）

**まだ確認できていないこと（次に触るときは、ここから）：**

- **アプリ内でやりたいことの名前の🔗を押したとき、複数のリンクが全部開くか。**
  ポップアップとして止められる可能性があり、その受け皿（「開けなかったもの」）も
  実機では未確認。**この機能の本題はここなので、最初に確かめること**
- `steam://` などの独自スキームが、ブラウザからそのまま起動できるか

**保留にした案（本人が「要るかどうか決める」と言った段階で止まっている）：**

- やりたいことに**起動キー（Ctrl+Alt+◯）をメモしておく欄**。
  いまアプリ側には「このやりたいことはキーで起動できる」と分かるものが何も無い

### 次にやること

**`SPEC.md` の「追加仕様：1週間の予定（2026-08-11 決定）」は、7項目すべて実装ずみ**（版 `2026-08-11.2`）。
未確認なのは**実機での使い勝手**だけ。まず本人に一度さわってもらうこと。

さわってもらうときに見てほしいところ：

- 1週間ページの**7列が、本人の画面幅で見やすいか**（狭いと2列に折り返す）
- **箱の始まる時刻**（設定タブ）が、本人の生活と合っているか。既定は平日18:00・休日10:00
- 「決まった時間」を設定したものが、**思った箱に入るか**

**`IDEAS.md`「案1：日記機能」は 2026-08-13 に実装した**（版 `2026-08-13.1`、下の「日記」）。
未確認なのは実機での使い勝手だけ。

**同期は 2026-08-14 に実装した**（版 `2026-08-14.3`、下の「同期」）。
ブラウザで2台ぶんの検証ずみ（受け渡し・log の足し算・取り消し・削除・合言葉違い・
圏外で直したぶんの保護）。**実機での確認はこれから。**

さわってもらうときに見てほしいところ：

- スマホとPCの両方で「つなぐ」→ **同じものが並ぶか**
- 昼にスマホで箱に入れ、夜にPCを開いて **並びがそのままか**
- 設定タブの「最後に合わせたのは」が更新されているか

---

## いまある機能

### 箱

- **1箱 = 30分。** 箱は時間そのもの。1箱に入るやりたいことは1つ
- 箱は個数と順番だけを持つ。**時刻は持たない**
- ただし「何番目がだいたい何時か」の**目安**は出す。設定タブで平日／休みの
  **1箱目の開始時刻**を決めると、ちょうど◯時のところにだけ線が出る（既定 平日18:00／休日10:00）。
  休憩を挟めば実時間はずれる。**目安であって、箱が時刻を持つようになったわけではない**
- その日の箱数は**基本値**（平日◯箱／休みの日◯箱、設定タブ）から始まる
- **どの日も「平日」から始まる。** 休みの日は**「今日の箱」の見出しの右**にある
  「平日／休み」を押して手で切り替える
  （曜日では判定しない。切り替えるとその区分の基本値に箱数が合わせ直される）
- `＋` `−` でその日だけ増減。**「今日の箱」の一番下**（最後の箱の下）にある。翌日はまた平日の基本値に戻る
- `−` を押すと記録が残り、トーストの「理由をつける」から**任意で**ラベルを付けられる
  （未入力のまま進める。設定タブに「減らした箱（今月）」として理由別に集計される）
- 箱を使い切ると「今日は終了」と明示される
- **残り箱数**は「今日の箱」の見出しに `残り N ・ 空き N` として出る
  （2026-08-15 に、画面上部の帯 `#boxBar` は廃止した。下の「画面」を読むこと）

### 1日の区切り

- **日付の切り替えは午前2時。** 深夜1時はまだ前日
- **午前 2:00–14:00 ／ 午後 14:00–翌2:00**（旧「朝4-12/夜18-4」は廃止ずみ）
- タブを開きっぱなしでも、15秒ごとに区切りをまたいだかを見て表示を切り替える

### やりたいこと

- 登録時に**必要な箱数**をセットで入力する（やりたいことに固定で持つ）。2箱のやりたいことは連続した2箱を占める
- **箱に入れる操作はドラッグ。** 右の一覧から左の箱へ。箱の中のものを別の箱へ動かすのもドラッグ
- 頻度つき（単発／毎日／週N回／月N回／N日にM回）
- **単発にも回数を持てる**（期限なしの総回数）。やり切ると「達成したもの」へ移り、リストから消える
- **✎ボタンで後から編集できる。** 名前・くり返し・回数・箱数・午前午後・曜日と時刻・まだ先の予定
- **曜日と時刻を決めておける。** 決めると、1週間ページでその日が作られたときに自動で箱に入る
- **まだ先の予定**（`remindAt`）。その日が来るまで、やることリストには出ない
- リンクは**1つのやりたいことに何本でも**持てる（下の「リンクとアプリ」）
- **実績**：箱に入れたやりたいことを完了すると、「今日やったこと」に実績（実際に何箱かかったか）の入力欄が出る。**任意入力。空のままでよい**
- 「今日はやらない」（見送り）、削除・記録・完了の取り消しも従来どおり

### リンクとアプリ（1つのやりたいことに何本でも）

やりたいことの名前の 🔗 を押すと、そのやりたいことに登録したものが**上から順に全部**開く。
一覧の 🔗 ボタン（「やりたいこと」タブ）で編集する。行は `↑` `↓` で並べ替えられ、**その順番が開く順**になる。

行は2種類。

| 種類 | 中身 | ブラウザから開けるか |
|---|---|---|
| **リンク** | `https://…`。開くブラウザ（既定／Chrome／Brave／Edge／Firefox）を選べる | 開ける。**ただし既定のブラウザでしか開かない** |
| **アプリ** | 独自スキーム（`steam://rungameid/…`、`discord://` など） | 開ける（「◯◯を開きますか？」が出る） |
| **アプリ** | exe のパス（`C:\Program Files\obs-studio\bin\64bit\obs64.exe`）＋引数 | **開けない。起動ファイル(.bat)が要る** |

#### ブラウザにできないこと（迂回する方法は無い）

1. **exe を起動する。** ページから任意のプログラムは実行できない
2. **開くブラウザを選ぶ。** `window.open` は必ず既定のブラウザで開く

この2つのために **起動ファイル(.bat) の書き出し**を付けてある。
リンク編集の「起動ファイル(.bat)を書き出す」、または「いま、これ」の「起動ファイル」ボタンから出る。
**同じブラウザ指定が連続する行は1つのコマンドにまとめる**ので、1つの窓にタブで並ぶ。

> **`start "" chrome "url"` のように名前で呼んではいけない（実際にやって外した）。**
> `chrome` が実行ファイルとして解決されないと、`start` はその引数をただのURLとして扱い、
> 結局**既定のブラウザで開く**。しかもエラーを出さずに開くので、
> 「Chrome指定にしたのに全部 Brave で開く」という形でしか気づけない。
> いまは `BROWSERS[].paths` の候補を `if not exist` で順に見て、
> **見つかった exe をフルパスで叩く**。どれも無ければ `echo` ＋ `pause` で止めて知らせる。
> **黙って既定にフォールバックさせないこと。**

自動で見つからないブラウザは、設定タブ「ブラウザの場所」に入れてもらう（`S.browserPaths`）。
入っていれば候補の先頭に来る。`%LocalAppData%` のような書き方も通したいので、
**この値は `%` を `%%` にしない**（URLとは扱いが違う）。

- 文字コードは **UTF-8（BOM無し）**。2行目の `chcp 65001 > nul` より後の行は cmd が UTF-8 として読む。
  日本語を含むパスのために必要なので消さないこと
- `%` は `%%` に、`"` は削っている（`batStr()`）。バッチの引数が壊れるため
- **exe は `start "" /d "そのexeのフォルダ" "…\app.exe"` で起動する。** `/d` を外すと
  バッチを置いた場所が作業フォルダになり、**OBS のように自分のフォルダから起動される前提の
  アプリが落ちる**。スキーム（`steam://` など）にはフォルダが無いので付けない
- 書き出したバッチは**その時点の写し**。アプリ側でリンクを変えても追随しない。
  `rem 書き出し: 日付` を入れてあるので、古いかどうかはそれで分かる

#### ポップアップの取りこぼし

2本目以降の `window.open` はブラウザにポップアップとして止められることがある。
止められたぶんは「開けなかったもの」ダイアログに出て、**1つずつ押せば確実に開く**。
そのページのポップアップを許可すれば、次からは1クリックで全部開く。

> `window.open(url, "_blank", "noopener")` は Chrome では**常に null を返す**ので、
> 開けたかどうかを見分けられない。`noopener` を付けずに開いて、あとから `w.opener = null` にしている。
> ここを「安全のため」と戻すと、全部が「開けなかった」扱いになる。

#### 開く処理は同期で書くこと

`openTask()` の中で `await` をはさむと、その後の `window.open` は
「ユーザー操作の外」と見なされて全部止められる。開き切ってから、ダイアログなどを出すこと。

### タイマー（2026-08-14 追加）

「いま、これ」の中にある。**1箱＝30分なので、既定は30分。**
`＋` `−` で**1分ずつ**増減し、**0になると音が鳴る**。
音は設定タブで好きなものに差し替えられる（下の「好きな音を入れる」）。

- ボタンは「始めるよ」→（動作中）「やめる」→（鳴ったあと）「もう一回」の3つの顔だけ。
  **一時停止は持たない**（「やめる」は設定した長さに戻すだけ）
- 動いている最中も `＋` `−` は効く。**残りは1分を下回らない**（上限8時間）
- 鳴ったら「時間になりました」＋枠が数回点滅し、**「音を止める」**が出る
- **音はどちらの場合も自分から止まる。鳴りっぱなしにはしない。**
  既定のピッピッピッは1.5秒おきに6回（`RING_TIMES`）、入れた音は10秒（`RING_SEC`）

> **状態はメモリだけ。localStorage に入れていない。**
> 残り時間まで保存すると「閉じている間も進んでいたことにするのか」を決める話になる。
> タイマーは座って使うものなので、**読み込み直したら30分に戻る**で通す（本人に確認すること）。

> **既定の音は音声ファイルを持たず、WebAudio でその場で作る**（`beep()`）。
> `file://` で動くことと、ファイルを増やさないため。
> **`AudioContext` は「ユーザーが押した流れ」の中でしか鳴らせない**ので、
> `startTimer()` の頭で `audioOn()` を呼んで起こしてある。ここを後回しにすると、
> 30分後に鳴らそうとしても**無音のまま何も起きない**（エラーも出ないので気づけない）。

#### 好きな音を入れる（設定タブ「タイマーの音」）

画像と同じ考え方で、**1つだけ**入れられる。入れなければ既定の「ピッピッピッ」。

- 保存は `S.timerSound = { name, data:dataURL, sec, rate, at }`（`null` なら既定）。
  **本体のデータ版は上げていない**（v7 のまま）。`normalize` で `data` が無ければ `null` に落とすだけ
- **入れた音は `RING_SEC`（10秒）だけ鳴る。** 10秒に足りなければ、その間くり返す
  （`src.loop = true` で持たせている。回数ではなく長さで止める）。
  既定のピッピッピッのときだけ、これまでどおり1.5秒おきに6回
- 置き場所は**設定タブ**にした。画像は「ごほうび＝気持ちのもの」だから哲学タブにあるが、
  こちらは鳴り物の設定なので設定タブに置いている

> **長い曲は、取り込むときに最初の10秒へ切り落とす**（`prepareSound()`）。
> 曲を丸ごと持つと localStorage（目安5MB）を画像と食い合って、
> **やりたいことが保存できなくなる**。上限は画像1枚と同じ 400KB（`SND_MAX`）。
>
> - **切るには作り直しが要る。** `OfflineAudioContext(1, 秒×rate, rate)` で鳴らし直すと、
>   頭だけ・モノラル・粗く、を一度に作れる。出てくるのは WAV（無圧縮）なので、
>   入らなければ `SND_RATES`（16k→12k→8k）の順に粗くする。画像の「画質→寸法」と同じ落とし方
> - **短くて軽いもの（10秒以内・400KB以下）は元のまま持つ。** mp3 を WAV に焼き直すと、
>   音が悪くなって容量も増えるだけなので、切る必要が無いなら触らない
> - 保存は `setTimerSound()`（`trySave()` で試して、駄目なら元に戻す）を通すこと。
>   画像の `addImage()` と同じ理由

> **鳴らす音は「始めるよ」で先に読んでおく**（`primeSound()` が `AudioBuffer` にして持つ）。
> 鳴る瞬間に読み始めると間に合わない。読めなかったときは**黙って既定のピッピッピッに落ちる**
> （`playSound()` が false を返す）。無音で終わらせないこと。

> **終わりは残り秒ではなく終了時刻（`timerEndAt`）で持つこと。**
> `setInterval` は遅れるので、残り秒を引き算していくとじわじわずれる。

> **1秒ごとの更新で `renderNow()` を呼ばないこと**（`paintTimer()` が数字だけ書き換える）。
> 毎秒作り直すと、押そうとしたボタンが消えて押せなくなる。
> 逆に、状態が変わるとき（開始・停止・鳴った）は `renderNow()` を通す。
> `renderNow()` は `timerHTML()` を出したあと **必ず `bindTimer()` を呼ぶ**（2か所ある）。

### 同期（2026-08-14 追加）

**ほかの端末と、やりたいこと・箱・日記を合わせる。** 設定タブ「ほかの端末と同期」。
仕様は `SPEC.md`「追加仕様：端末をまたいで使う」、置き場の手順は `sync/README.md`。

- 置き場は **https://iamidle.vercel.app/api/sync**（`SYNC_URL_DEFAULT`）。
  設定タブの欄に既定として出るので、端末ごとに打ち直さなくてよい。書き換えればそちらが使われる
- **綴りを `iamidol` → `iamidle` に直した（2026-08-17）。** GitHub と Vercel の両方を改名ずみ。
  端末の打ち直しは不要だった。記録の移行も不要（ローカルはホスト名ごとの保管なので
  `/iamidol/` と `/iamidle/` で同じものを見る。サーバー側は名前で引いていてアドレスと無関係）
- **Vercel に上げているのは `api/` だけ**（`.vercelignore`、2026-08-17 に追加）。
  それまではアプリ本体も並んでいて `vercel.app` からも開けた。ホスト名が違えば
  `localStorage` は別の引き出しなので、開く入口は GitHub Pages の1つだけに絞ってある。
  `.vercelignore` に `api/` を書くと Function ごと消えて同期が止まるので注意
- ⚠️ **Vercel の旧ドメイン `iamidol.vercel.app` を消さないこと。**
  新旧2つを両方とも本番に割り当ててあり、どちらも同じ Function に届く。
  既定値が使われるのは設定タブが空のときだけで（`app.js` の `SY.url || SYNC_URL_DEFAULT`）、
  一度保存した端末には旧アドレスが文字列で残るため、**旧ドメインを消すとその端末が繋がらなくなる**。
  掃除のつもりで消すと、いちばん気づきにくい形で同期が止まる
- ⚠️ `SYNC_SALT` / `SALT_PREFIX` の **`"iamidol.sync.v1:"` は綴り間違いのまま据え置き**。
  これは名前ではなく鍵を作る材料で、直すと今までの中身が開けなくなる
- 使っているのは **`niku` と `Haru` の2人**。引き出しは完全に分かれている
- **同期するのは** やりたいこと／箱／ルーティーンと実施記録／基本値と開始時刻／
  今週だけの回数／哲学・一言／日記
- **同期しないのは** 画像・タイマーの音・ブラウザの場所（重い、または端末ごとのもの）

#### 中身は読めない

```
合言葉（端末から出ない）
  ├→ 入口用（auth）  … サーバーに送る。「この人だ」の確認だけ
  ├→ 中身の鍵（enc） … 送らない。AES-GCM で包む
  └→ 見出しの鍵（id）… 送らない。日付やIDを HMAC で伏せる
```

サーバーに残るのは **名札・種類・伏せた見出し・触った時刻・暗号文**だけ。
**Supabase を持っている本人でも、`Haru` の中身も「いつ書いたか」も読めない。**

> **作り方（塩・回数）は `sync/key.html` と `app.js` の2か所にある。片方だけ直すと、
> それまでのデータが二度と開けなくなる。** 塩に名前を混ぜてあるので、
> 同じ合言葉を2人が選んでも別の鍵になる。

> **合言葉が違うときは、何もせずに止まる。** `check` という1行だけ**見出しを伏せていない**
> ので、鍵が違っても必ず見つかる。それが開けなければ「合言葉が違います」を出して、
> **1件も入れず、1件も送らない**。ここを伏せると、鍵違いの端末が「知らない行だらけ」と
> 判断して、ゴミを大量に送りつけることになる。

#### 変わったかどうかは「印」で見る

**`days` や `tasks` に「触った時刻」を持たせていない**（`SPEC.md` の当初案から変えた）。
書き換えるところ全部に時刻を足すと、**1か所でも漏らした瞬間にそこだけ同期されなくなる**。
代わりに、前回合わせたときの**中身の印**（`SY.marks`）を控えておいて、
印が変わっていたら「直した」とみなす。**`save()` を通れば必ず拾えるので、漏れない。**

- 控えは `localStorage["jiko-kanri-sync-v1"]`。**`S` にも書き出しにも混ぜない**
  （合言葉もここに入るので、バックアップJSONには出ない）
- 送るのは印が変わったものだけ。前はあったのに今無いものは**墓標**（`body = null`）

#### 落とし穴（実際に手当てしてあるもの）

| | 中身 |
|---|---|
| **受け取りと送りを同じ回にしない** | まとめると、その日を作ったばかりの端末が、もう一方の並べた箱を上書きする。**必ず「受け取る→入れる→送る」の順**（`syncNow()` が2回に分けて呼んでいる） |
| **やりたいことを箱より先に入れる** | 逆にすると `getDay()` が「知らないID」として箱を片付けてしまう（`KIND_ORDER`） |
| **取り消しが足し戻される** | `log` は足し算なので、取り消しただけでは、もう一方から戻ってくる。**取り消しは必ず `dropLog()` を通し、`gone` に控える** |
| **圏外で直したぶんが消される** | 届いたものを何も見ずに入れると、まだ送れていない直しが古いもので潰れる。**こちらが直してあって、かつ `SY.dirtyAt` の方が新しければ入れない**（記録だけは足す） |
| **画像の選択が飛ぶ** | `day` を送るときは `img` を落とし、受けるときは手元の `img` を残す |

#### 最初の1回

**先に同期した端末の中身が優先される。** つなぐ前に、両方の端末で
「バックアップ書き出し」をしておくのが安全（つなぐ確認ダイアログにもそう出る）。

### ごほうびの画像

- **哲学タブ**でお気に入りの画像を保存する。**最大8枚**（`IMG_LIMIT`）
  （もとは設定タブにあった。ごほうび＝気持ちのものなので哲学タブへ移した）
- **1枚 400KB 以下に自動で圧縮**して持つ
  （長辺1600pxに縮め、JPEG画質 → 寸法の順に落として 400KB を切るまで繰り返す）
- その日に出る1枚は、保存した中からランダムに選ばれ、**日付が変わるまで変わらない**
  （`days[key].img` に覚える。その画像を消したら選び直す）
- 出方は3段階
  1. 最初は「今日の箱」の背景に敷かれるが、**箱に隠れて見えない**
  2. **終わった箱が透けて**、そこだけ画像が見える
  3. **箱を使い切ると「いま、これ」へ移り**、大きくはっきり出る（箱の背景からは消える）
     - そこでは文字とボタンが画像に重なって見えないので、**右上の目のボタンで全部消せる**
       （`rewardBare`。メモリだけに持ち、保存しない。画像が引っ込んだら戻る）

> **2026-08-18：空き箱の膜。** 箱を全部は埋められない日でも画像にたどり着けるようにした。
> **画像を隠すのは「やると決めて、まだやっていない箱」だけ**、という考え方に変えている。
> 空き箱は破った約束ではなく、ただ割り当てなかった時間なので、同じ重さで塞がない。
>
> - 空き箱にかかる膜の濃さは `--emptyveil`。`renderBoxes()` が毎回入れる
> - 濃さは **`usedBoxes(d) / filled`**（＝**入れた箱**のうち終わった割合）で 0.88 → 0.18
> - **分母は `d.count` ではなく `filled`**。ここが肝で、全部の箱を分母にすると
>   埋め切れなかった日は何をやっても薄くならず、画像にたどり着けない
> - やりたいことを新しく箱に入れると分母が増えるので、膜はいったん濃くなる。
>   「入れたぶんのうち何割やったか」なので、これで正しい
> - `dayFinished()` は**触っていない**。「今日は終了」も「いま、これ」への昇格も、
>   これまでどおり箱を使い切ったときだけ
> - `.boxrow.empty.over`（ドラッグの落とし先）は膜より強く出すこと。
>   書かないと膜に負けて、どこに落とすのか分からなくなる

> **容量の注意。** 画像もやりたいことと同じ localStorage に入る（Chrome は `file://` で
> IndexedDB が使えないため）。目安は 5MB。**400KB × 8枚 ＝ 約3.2MB** に収まるよう
> 枚数を8枚で止めている。
> **画像のせいでやりたいことが保存できなくなってはいけない。**
> 追加は必ず `addImage()`（`trySave()` で試して、駄目なら足したぶんを戻す）を通すこと。
> 設定タブに保存量の目安を出してある。

### ルーレット（決めるのが面倒なとき用）

- **ホーム右カラムの一番上。スクロールしても、そこに貼り付いたまま動かない**
  （2026-08-15。やりたいことが増えるとドラッグの落とし先が画面から消えるため。
  仕掛けは上の「画面」の `#rouletteBox` を読むこと）
- やりたいことを**ドラッグして候補に足す**
- 「回す」で名前が高速に切り替わり、だんだん遅くなって1つに決まる
- 結果には「これを箱に入れる」が付く。入れた候補はルーレットから外れる
- 候補はその日の記録（`days[key].roulette`）。日付が変われば空になる
- 選ばれたやりたいこと（`winner`）は保存しない。その場かぎり

### やったときの一言（ドーン）

- やりたいことを完了すると、登録した一言から1つが画面いっぱいに出て1.1秒で消える
- 登録・削除・試し表示は**哲学タブの「やったときの一言」**
- 初回だけ5つ入れてある。全部消したら何も出ない（消したものは復活しない）
- **`pointer-events:none` を外さないこと。** 消えるまで「取り消す」が押せなくなる

### ルーティーン

- **箱を消費しない。** やりたいこととは別枠
- 午前用・午後用の2種類。いまの時間帯のものだけがホームに出る
- 日付が変わればリセット（`routineDone` が日付キーなので自動）

### 日記（2026-08-13 追加）

`IDEAS.md`「案1」の実装。**Phase 1（この端末の中だけ）まで**が入っている。

**本人が決めたこと：**

| 点 | 決定 |
|---|---|
| 入力欄の形 | **③ 既定は1欄。必要なときだけ他を開く**（`IDEAS.md` の3案から本人が選択） |
| 箱 | **消費しない。** ルーティーンと同じ別枠。やりたいことに入れるかは本人の自由 |
| 画面 | **日記タブを新規追加**（タブは5つ→6つ）。上に今日の入力欄、下に過去 |
| 書かなかった年 | **出さない。** あるときだけ出す |
| 外部との連携 | **いまは無し。** あとから足す（下記） |
| 暗号化 | **しない**（本人判断） |
| 保存 | **`保存する` を押したときだけ**（2026-08-15 に自動保存をやめた）。同じ日に何件でも書ける |

**欄は4つ。全部任意で、書いた欄だけが残る。**
「今日できたこと」だけが最初から開いていて、「もやもや」「今日の気持ち」「日常に感謝」は
`＋` を押したときだけ出る。中身がある欄は次に開いたとき自動で開く。
**閉じる操作は用意していない**（＝「開くだけ」。畳んだ中に書いたものが隠れる状態を作らない）。

> ### 保存先は本体と別のキー。ここは意図的
>
> **`jiko-kanri-diary-v1`**（本体は `jiko-kanri-v1`）。分けた理由は3つあり、どれも実際に効く：
>
> 1. **`days` の180日 `prune()` に巻き込まれない。** 日記は何年でも残すもの
> 2. **`migrate()` を通らない。** だから日記のためにデータ版を上げていない（**v7 のまま**）。
>    v6 の移行で本体を丸ごと空にした事故があるので、その経路に日記を乗せない
> 3. **本体JSONが壊れて `-broken` 送りになっても、日記だけは生き残る**
>
> ただし **localStorage の容量はオリジン単位で共有**なので、これで容量が増えるわけではない。
> 何年も貯めるぶんは、画像（最大3.2MB）と食い合う。設定タブの「保存量のめやす」に
> 日記ぶんも内訳で出してある。

```js
// localStorage["jiko-kanri-diary-v1"]
{ v:1, entries: {
    "2026-08-13":              { done, mood, gripe, thanks, at },   // 1日1件だった頃のもの
    "2026-08-15#1755230400000":{ done, mood, gripe, thanks, at }    // いまの形（日付 + "#" + 保存した時刻）
} }
```

> ### 保存キーは2つの形が混ざる。移行はしない（2026-08-15）
>
> 「同じ日に何件でも書ける」ようにしたとき、キーを **`日付 + "#" + Date.now()`** に変えた。
> **古い `"2026-08-13"` の形はそのまま残して読む。** 書き換える移行は入れていない：
>
> - どちらの形も**先頭10文字が日付**なので、`Object.keys().sort()` の並び順が日付順のまま変わらない
> - 同期は**キーをそのままID**にして送っている。書き換えると、
>   もう一方の端末には「消えた1件＋知らない1件」に見えて、墓標と復活で事故になる
>
> **日付を取り出すときは必ず `diaryDateOf(key)`（＝先頭10文字）を通すこと。**
> `dayStart()` は `"2026-08-13"` の形しか受け付けず、`okKey()` で**黙って今日に化ける**。

> **`gripe` は昔の呼び名（グチ）のまま。** 画面の見出しだけ「今日の気持ち」に変えた。
> 保存名を変えると、これまで書いたものが読めなくなるため。
> 見出しは `DIARY_FIELDS` の `label` 1か所（＋ index.html の初期表示2か所）で決まる。

> **欄を足すときは3か所。**（`thanks`「日常に感謝」を 2026-08-14 に足したときの手順）
> `app.js` の `DIARY_FIELDS` に1行 ／ `index.html` の `.dfield`（`id` は `キー名Wrap` の形） ／
> `index.html` の `.dmore` に `＋` ボタン（`data-open` は保存名と同じ）。
> **移行は要らない。** 書いていない欄はキーごと持たないので、
> 古い日記は「その欄が無い日」として、そのまま読める。過去の日の表示・書き直しも
> `DIARY_FIELDS` を回しているだけなので自動で付いてくる。

- **空の欄はキーごと持たない。全部空なら、その1件ごと消える。**
  「書かなかった日」がデータに残らないので、過去を並べると自動で「書いた日だけ」になる
- 日付は本体と同じ **`dayKey()`（午前2時区切り）**。深夜1時に押したものは前日の日記
- 保存は **`保存する` を押したときだけ**（下の「自動保存をやめた」）
- 過去のものは ✎ でその場が入力欄に変わる（**あとから書き足せる**）。✕ で削除（取り消しつき）。
  **書き直しても `at` は付け替えない**（`at` は一覧に「書いた時刻」として出しているため）
- 過去は「**同じ日に書いたもの**」（**別の年**の同じ月日。`1年前` のラベルつき）→
  「**これまでの日記**」（今日のぶんもここの先頭。20件ずつ「もっと見る」）の順

> ### 年月のしぼりこみ（2026-08-16）
>
> 「これまでの日記」の見出しの右に `<input type="month">` を置いてある。`diaryMonth`
> （`""` なら全部、`"2026-08"` ならその月だけ）で `rest` を切る。
>
> - **入力は `index.html` に静的に置いてある。`renderDiaryPast()` で作り直さないこと。**
>   作り直すと、年を打っている途中で `input` が消えてフォーカスが飛ぶ。
>   だから過去のものは**2つの入れ物**に分かれている ——
>   `#diarySame`（同じ日に書いたもの）／`#diaryPastHead`（見出しと入力・静的）／`#diaryPast`（一覧）
> - **カードが2か所にあるので、✎ と ✕ の click は `#sec-diary` で受けている**（`.dcard` の中だけ通す）
> - **「同じ日に書いたもの」はしぼりこみを受けない。** 年をまたいで出したいもの（本人の指示）
> - `min` / `max` は書いたものがある月の幅（`diaryMonthSpan()`）。カレンダーで無い月を選ばせない
> - **1件も無いときは見出しごと隠す。** 逆に、しぼりこんだ月が空でも**見出しは残す** ——
>   消すと「すべて」が押せなくなって戻れなくなる
> - **保存すると `diaryMonth` は解ける。** 古い月でしぼったままだと、いま保存したものが下に出ない
- バックアップの書き出しは、本体に `diary` を足した1ファイルにまとめている。
  読み込み側は **`diary` を取り出してから `delete d.diary`** している。
  **本体（`S`）に日記を混ぜないこと**（混ぜると `save()` のたびに日記ごと書き直すことになる）
- **日記が入っていない古い書き出しを読み込んでも、いまの日記は消さない**（確認ダイアログにそう出る）

#### 自動保存をやめた（2026-08-15・本人の指示）

以前は打つそばから自動で保存していた（700ms デバウンス＋`blur`＋`beforeunload`）。
**やめて、`保存する` ボタン1つにした。** 変わったのは3点：

| 点 | 前 | いま |
|---|---|---|
| 保存 | 打つそばから自動 | **`#diarySaveBtn` を押したときだけ** |
| 1日の件数 | **1件**（同じ日に書くと上書き） | **何件でも**（押すたびに新しい1件） |
| 上の入力欄 | その日に保存したものの**写し**だった | **保存前の下書き専用**。保存すると空になる |

- 押すと `saveNewDiary()` が `newDiaryKey()` で新しいキーを作って足し、
  **その場で `renderDiaryPast()` を呼ぶ**ので、すぐ下の一覧の先頭に出る
- **入力欄に保存ずみの中身を入れ直さないこと。** `renderDiaryHead()` は見出しと畳みだけを触る。
  値を代入すると、タブを行き来しただけで打ちかけが消える（`fillDiaryToday()` はこれで廃止した）
- `localStorage` が満杯で保存できなかったときは、**足した1件を `delete` して打った文字は欄に残す**
- 見出しの右（`#diaryState`）は**保存したときだけ**「保存しました hh:mm（緑）」。
  2026-08-16 に本人の指示で「保存していません」は消した（打っている最中は何も出さない）。
  合わせて、入力欄の下にあった注意書き（`.dnote`）も消してある
- `beforeunload` は「打ちかけがあるときブラウザに引き止めさせる」だけになった（**もう保存しない**）。
  **画面には何も出ないので、押し忘れの受け皿はこれだけ**。外さないこと
- **打ちかけは書き出し（エクスポート）にも同期にも入らない。** 押していないものは、まだ存在しない

> **日付をまたぐときの落とし穴は、これで消えた。**
> 以前は `diaryKeyOpen`（入力欄に出している日）を持って「2:00をまたいだら、まず前の日として確定」
> していた。いまは押した瞬間の `dayKey()` に入れるだけでよい
> （＝**「いつ書いたか」ではなく「いつ保存したか」**で決まる、と割り切った）。

同じ理由で `renderDiary()` は**先に打ちかけを確定してから**欄を埋め直す。
`renderAll()` は `visibilitychange` でも走るので、これが無いと打ちかけが消える。
欄への代入は**値が違うときだけ**（同じ値を入れ直すとカーソルが末尾に飛ぶ）。

#### あとから足すときのために（Phase 2・未着手）

本人と相談して決めた方針だけ残す。**まだ何も書いていない。**

- **localStorage を正のままにして、Vercel の Function 経由で Supabase へミラーする**
  （`IDEAS.md` の案3の自動化版）。オフラインでも書けることを崩さない
- **Supabase を直に叩かない。** anon key は公開ソースに載ってしまう。
  service_role キーは Function の環境変数に置き、アプリからは**合言葉**（初回だけ入力、
  以後 localStorage）をヘッダに載せて Function を叩く。
  **鍵を隠すだけでは足りない。無認証の Function は、URLを知っていれば誰でも読める**
- Vercel 自体はDBを持たない（Postgres も KV もマーケットプレイス経由）。保存先は Supabase のままでよい
- **アプリ本体を Vercel へ移すのは別件。** オリジンが変わると localStorage は空に見えるので、
  やるなら書き出し→読み込みが必須。日記と同時にやらないこと
- いまの実装は Phase 2 を足しても**作り直しにならない**。`saveDiary()` の後ろに送信を挟むだけ

### 1週間ページ

- **週は日曜はじまり。** アプリ全体（「週に何回」の集計も）これでそろえてある（`weekStartKey()`）
- **今週／来週の切り替え**で見る。2週間を並べては出さない
- 各日に**仕事／休み**、箱の `＋` `−`。やりたいことリストから**ドラッグ**してどの日の箱にも入れられる
- **過ぎた日には入れられない**（過去は記録なので触らない）

#### 達成の数え方（行動ベース）

達成は**その日に達成ボタンを押したとき**に数える。箱に置いただけでは達成にならない。
リストに出すかどうかは、この式だけで決まる。

```
残り ＝ 回数 － その期間にやった数 － これから来る箱に置いてある数
```

- 置ききると**リストから消える**（「入れきったら、もう終わり」）
- **サボった分は勝手に戻ってくる。** 過ぎた日の未実行の箱は「これから来る箱」に入らないため
- **すでにやった日の箱も数に入れないこと**（`plannedAhead()`）。
  ここを数えると、やった数と二重に引いて**残りが負になる**（実際に踏んだ）

#### 自動配置

- 「決まった時間」を持つものは、**その日が作られたときの1回だけ**自動で箱に入る（`ensureDay()`）
- **それ以降アプリは箱に手を出さない。** 本人が外したものを置き直すこともしない
- 置けなかったときは**ずらさない**。1週間ページの上に**アラート**を出して本人に直してもらう
- 例外：**編集で時間を決めた／変えた直後**だけ、もう作られている先の日にも入れる（`autoPlaceAhead()`）。
  これが無いと、いま開いている週には永久に入らない。**本人の操作の続きなので、勝手に動かすのとは別**

#### 今週だけの抜け道

- 「週に何回」のものは、1週間ページの一番上の帯で `＋` `−` して**その週だけ**回数を変えられる
- `S.weekOverrides` に**週キー**で持つ。**週が変われば `prune()` が捨てて、もとの回数に戻る**

### 画面

- 下部タブは**6つ**。左から**今日／日記／1週間／やりたいこと／哲学／設定**
  （並び順は `index.html` の `<nav>` の順そのまま。JS は `data-tab` で引くので順番に依存しない）
- **哲学タブ**は「人生哲学」「やったときの一言」「お気に入りの画像」の3つ
- ホームだけ2カラム。**左＝今日の指針＋いま、これ＋今日の箱の列**（＋見送り・今日やったこと）／
  **右＝ルーレット＋箱に入れていないやりたいこと＋ルーティーン**（＋キーボードの説明）
- 幅900px以下では1カラムに畳む

> ### 今日タブは枠を画面に固定して、左右それぞれの中でスクロールする（2026-08-15）
>
> **`switchTab()` が `body` に `now-fixed` を付け外しする。** 今日タブのときだけ付く。
> ほかのタブは、これまでどおりページ全体がスクロールする。
>
> - `body.now-fixed` … `height:100dvh` ＋ `overflow:hidden`（ページ自体は動かなくなる）
> - `.wrap` は縦の flex。`padding-bottom:var(--navh)` で**下のタブ帯（`position:fixed`）のぶんを空ける**。
>   **タブ帯の高さを変えたら `--navh` も直すこと**（`:root` にある）
> - `.home .col` に `overflow-y:auto`。**`min-height:0` が要る** ——
>   grid/flex の子は既定 `min-height:auto` で中身ぶんまで伸びてしまい、`overflow` が効かない
> - **幅900px以下では固定をやめる**（1カラムだと縦に長く、ページごと動いたほうが読める）
>
> **哲学バー（今日の指針）は左カラムの中に入れた。** 以前は2カラムの外（全幅）にあった。
> 全幅に戻すなら `.home` の外に出すことになるが、そのぶん左右のスクロール枠が縮む。
>
> #### ルーレットは右カラムの一番上に貼り付く（2026-08-15）
>
> やりたいことが増えると、下までスクロールしたときに**ドラッグの落とし先が画面から消える**。
> それを避けるため `#rouletteBox` を `position:sticky; top:0` にしてある。
>
> - **貼り付く相手は「スクロールしている枠」＝ `.col`。**
>   だから `.col` が `overflow` を持つ `body.now-fixed` のときだけ効く。
>   **幅900px以下では `position:static` に解いている** ——
>   解かないと貼り付く相手が画面そのものになり、ヘッダーの上に居座る
> - **下地（`background:var(--bg)`）が要る。** パネルどうしの隙間（`margin-bottom:14px`）から
>   下の中身が透けて見えるため
> - **丈を `max-height:50%` で切ってある。** 候補が10件も入るとルーレットだけで枠が埋まり、
>   ドラッグの元になる一覧に手が届かなくなる。切ったぶんはルーレットの中でスクロールする。
>   落とし先（`#rouDrop`）はパネルの先頭にあるので、**丈を切っても落とし先は必ず見えている**

- **哲学バーは「今日」タブの中にある。** 他のタブでは出ない
- 画面上部に貼り付いていた**残り箱バー（`#boxBar` / `.boxbar`）は 2026-08-15 に廃止**。
  中身は「今日の箱」に畳んだ（`renderBoxes()` の `.bhead` と `.bfoot`）。
  `renderBoxBar()` も消したので、**`renderAll()` から呼ばないこと**
- **1週間ページは1カラム。** 「この週に置くもの」を**一番上**に帯として置き、
  スクロールしても上に貼り付く（`.weekside` が `position:sticky`）。その下に7日ぶんの箱の列

> ### 「この週に置くもの」は2段で折り返す（2026-08-16）
>
> もとは**横1列に並べて横スクロール**させていた。件数が増えると横に長くなりすぎて
> ドラッグする前に横に送るのが面倒、と本人。**折り返して2段ぶんの高さに収め、
> あふれたぶんは縦に転がす**形にした（`.wksidelist` は `flex-wrap:wrap` ＋ `overflow-y:auto`）。
>
> - **`max-height:94px` は「1件43px ＋ すきま6px」の2段ぶち抜き。**
>   1件の丈を変えたら、ここも数え直すこと。半端だと2段目が切れて、
>   見える件数が5件に落ちる（実際にやった）
> - **1件は `width:206px` に固定し、名前も下の行も1行で打ち切っている**
>   （`.nm` / `.s` の `text-overflow:ellipsis`）。
>   **折り返させないこと。** 丈が揃わなくなって、2段に収めた枠から溢れる。
>   全部は `.item` の `title`（マウスを乗せると出る）で読める
> - 1280px幅で **1段5件・2段で10件見える**。900px以下でも横スクロールにはならず、1段2件で折り返す
- **キーボード**（「今日」タブでのみ有効。入力欄にフォーカスがあるときは無効）
  - `Space` / `Enter` … いま、これを完了
  - `N` / `↓` … 次の箱へ　`P` / `↑` … 前の箱へ
  - `+` … 箱を増やす　`-` … 箱を減らす

### バージョンと更新（GitHub Pages 対策）

GitHub Pages は `index.html` も `css` / `js` も **10分ほどキャッシュされる**。
とくにブックマークから開いた場合は**サーバに1件も問い合わせず**、まるごと古い版で動く。

- `css` / `js` は `?v=<版>` 付きで読む。新しい `index.html` が届けば、中身も必ず一緒に入れ替わる
- 起動時に `index.html?r=<時刻>` を `cache:"no-store"` で1回だけ取りにいき、公開されている版と
  いま動いている版を比べる。違えば**上部に「新しい版があります」**を出す
- 画面（`meta app-version`）と中身（`APP_VERSION`）の食い違いも見る。
  片方だけ新しいという、ちぐはぐなキャッシュを拾うため
- 「最新に更新」は `?r=<時刻>`（まだ使われていないURL）へ移ることで `index.html` を必ず取り直す。
  Service Worker と Cache Storage も、あれば消す
- **この操作で localStorage は消えない。** データとキャッシュは別物

> **版を上げるときは4か所を必ず揃えること。**
> `index.html` の `meta app-version` ／ `style.css?v=` ／ `app.js?v=` ／ `app.js` の `APP_VERSION`。
> 揃っていないと「新しい版があります」が出っぱなしになる。

### そのほか

- 人生哲学（上部に1つ表示、クリックで次へ、日付が変わると自動で進む）
- バックアップ（JSONの書き出し／読み込み）

---

## SPEC の未決事項に対する答え（本人が決定ずみ）

`SPEC.md`「まだ決めていないこと」の4点は、本人の指示で以下に決まった。
**`SPEC.md` 本体は書き換えていない。**

| SPECの未決事項 | 決定 |
|---|---|
| 1. やりたいことを箱に入れる操作 | **ドラッグ。** 右の一覧から箱へドラッグして入れる。箱に入っているものを別の箱へドラッグして動かせる。クリックで入れる操作は無い |
| 2. 実績をどう見せるか | **溜める ＋ 完了時に一言をドーンと出す。** 一言は本人が登録する（哲学タブ「やったときの一言」）。数値の集計画面は作っていない。あわせて、やり切ると画像がごほうびとして出る |
| 3. 使い切ったあと何かやった場合 | `＋` で箱を足せば続けられる。あわせて**寝ることを促す一言**を出す（`SLEEP_LINE`。**仮案なので差し替え可**） |
| 4. 平日／休日の判定 | **曜日では決めない。** どの日も平日（仕事）から始まり、休みの日だけ箱バーの「平日／休み」を押して手で切り替える |

---

## データ

- **localStorage**、キーは `jiko-kanri-v1`。値はJSON1つ。**いまのバージョンは v7**
- **日記だけは別のキー `jiko-kanri-diary-v1`。** 上の「日記」を読むこと。
  `prune()` にも `migrate()` にも触れないので、**日記を足してもデータ版は上がっていない**
- オリジン単位で保存される。**ローカル（file://）とGitHub Pagesは別々の引き出し**で、同期しない
- **常に最新の状態が保存されていればよい。過去のデータに戻す要件は無い**（本人確認ずみ）
- コードのgit巻き戻しとデータは無関係。コードを戻してもやりたいことは消えない

### v7 の形

```js
{
  v: 7,
  // done / doneAt は v7 で廃止。単発も log[] に積み、goal 回ぶんで達成
  tasks: [{ id, text, links, when:"any"|"am"|"pm", freq, size,
            goal,                        // 単発の総回数（期限なし）。くり返しは 1
            fixed: null | { dow: 0-6|null, time:"19:00" },   // dow が null なら毎日
            remindAt: ""|"2027-04-01",   // この日まで、やることリストに出さない
            log:[ts], actuals:[{ts,day,planned,actual}], skipDay }],
  // links の1行は次のどちらか。並び順がそのまま開く順
  //   { id, kind:"url", url, browser:""|"chrome"|"brave"|"edge"|"firefox" }
  //   { id, kind:"app", path, args }     path は exe のパス、または steam:// などのスキーム
  cheers: [{ id, text }],                       // やったときの一言
  images: [{ id, data:dataURL, at }],           // ごほうびの画像（1枚400KB以下）
  routines: [{ id, text, slot:"am"|"pm" }],
  routineDone: { "2026-08-09": [routineId] },
  days: { "2026-08-09": { count, holiday, slots:[taskId|null],
                          cuts:[{at,label}], roulette:[taskId], img } },  // slots の長さ = count
  base: { weekday, holiday },
  boxStart: { weekday:"18:00", holiday:"10:00" },   // 1箱目の開始時刻（時刻の目安に使うだけ）
  weekOverrides: { "W2026-08-09": { taskId: 3 } },  // その週だけの回数。週が変われば prune が捨てる
  browserPaths: { chrome, brave, edge, firefox },   // 起動ファイル用。空なら自動で探す
  timerSound: null | { name, data:dataURL, sec, rate, at },  // タイマーの音。null なら既定のピッピッピッ
  philos: [{id,text}], philoIdx, lastOpen, foldDone, foldSkip
}
```

`days[key]` には `conflicts:[{id,text,time,why}]` が付くことがある。
自動配置で**置けなかったもの**の控えで、1週間ページのアラートを消すと消える。

- **移行は自動。** v1（習慣つき）→ v2（頻度つきのやりたいこと）→ v3（箱モデル）→ v4（手動の休み・一言）
  → v5（ごほうびの画像）→ v6（リンク複数化）を `migrate()` が順に通す
  - 朝 → `am`、夜 → `pm` に読み替える
  - 既存のやりたいことの `size` は 1箱 から始まる
  - v3 までに作られた日は、土日だったものを `holiday:true` として引き継ぐ
  - v5 までの `t.url`（1本）は `t.links[0]` に移して `t.url` を消す
- `days` は180日、`routineDone` は60日で古いものから捨てる（`prune()`）
- **`migrate()` は `load()` の途中で動く。** `uid` や `CUT` のような、あとで `const` 宣言される
  ものに触れると TDZ で落ちる。落ちると `catch` が初期状態を返し、**実データを空で上書きする**。
  移行処理では、あとで宣言されるものを使わないこと
  - **v6 の実装で実際に踏んだ。** `migrate()` から呼ぶ `normLink()` が `BROWSERS`（当初は
    ファイル下部の `const`）を見ていたため、v5 のデータで起動すると丸ごと空になった。
    そのため `lid()` と `BROWSERS` は **storage の節の頭に置いてある。下へ動かさないこと**
  - コンソールから `migrate()` を叩くテストでは**この事故は絶対に出ない**（そのころには
    下の `const` が初期化ずみだから）。**必ず、実データの形を localStorage に入れてから
    ページを開いて確かめること**
- **読めなかったデータは `KEY + "-broken"` に退避される。** `load()` の `catch` は
  初期状態を返すが、その前に生の文字列を横へ写し、起動時にトーストで知らせる。
  事故ったときは、そこから中身を取り戻せる

> **本人の実データが入っている。** テストで壊さないこと。下の「検証のやり方」を必ず読むこと。

---

## 開発上のハマりどころ（実際に踏んだもの）

### `confirm()` / `prompt()` が効かない
プレビュー環境ではネイティブダイアログが無効化されていて、**呼ぶと即座に `false` を返す**。
そのせいで削除ボタンが無反応になる事故が起きた。
→ アプリ内モーダル（`openModal` / `askConfirm` / `askInput`）を実装ずみ。**今後もネイティブダイアログは使わないこと。**

### `onclick` に関数をそのまま渡すと、イベントが引数に入る

`$("boxPlus").onclick = addBox;` と書くと、`addBox(key)` の `key` に **クリックイベントが渡る**。
`addBox()` が引数なしだった頃は問題なかったが、1週間ページのために `key` を足した時点で壊れた。
結果、`S.days["[object MouseEvent]"]` という存在しない日が作られ、
`dayLabel()` の中で `key.split is not a function` を投げて、**今日タブの ＋− が無反応**になっていた。

- 手当て1：`$("boxPlus").onclick = ()=> addBox();` と**必ず包む**
- 手当て2：`okKey()`（`/^\d{4}-\d{2}-\d{2}$/` に合わなければ今日にする）を通してから使う。
  `dayStart()` / `getDay()` / `dayLabel()` / `ensureDay()` の入口に入れてある
- 手当て3：`prune()` が日付の形でないキーを捨てる（すでに壊れたデータの後始末）

### 検証で本人のデータを壊さない
過去に2回、テストデータで実データを上書きしかけた。安全なやり方：

```js
// localStorage に書かずに描画だけ検証する
const real = S;                          // 実データを退避
S = { /* 合成データ */ };                 // 差し替え（save() は呼ばない）
renderNow(); renderBoxes();              // 描画を確認
S = real; renderAll();                   // 元に戻す
```

- `save()` を呼ばなければ localStorage は変わらない
- 関数単体（`todayNeed`, `statOf`, `placeTask` など）は合成オブジェクトを渡せばそのまま検証できる
- `nowSlot` / `dayKey` のような時刻依存の関数は、テスト側で時計を固定して確認するのが早い
  （Playwright なら `page.clock.install({time})`。午前2時のまたぎはこれで確認した）

### 本人がプレビューを同時に操作していることがある
ページのメモリ上の状態と localStorage がずれる。`location.reload()` の前には `save()` を呼ぶこと。

### その他
- **`file://` で動くこと**が必須。`type="module"` は使わない（CORSで動かなくなる）
- `app.js` が `S` をグローバルに宣言している。評価用スクリプトで `const S` を再宣言すると `SyntaxError` になるので、別名を使うか即時関数で囲む
- `getDay()` は、消えたやりたいことのIDが箱やルーレットに残っていたら黙って片付ける。
  テストで実在しないIDを箱に置いても消えるので、ふさぎ役には**実在するやりたいこと**を使うこと

### Windows のドライブレターはスキームに見える

`isScheme()` を `/^[a-z][a-z0-9+.\-]*:/` で書くと `C:\…\obs64.exe` の `C:` に当たってしまい、
exe が「独自スキームだから、そのまま起動できる」と誤判定される（実装中に踏んだ）。
**頭は2文字以上**（`+` であって `*` ではない）にしてある。1文字のスキームは無いものとして扱う。

### ドラッグの `effectAllowed` と `dropEffect` を食い違わせない
箱へは `move`、ルーレットへは `copy` で落としている。
`dragstart` 側を `effectAllowed="move"` にすると、`dropEffect="copy"` の受け皿では
**`drop` イベントが起きない**（`dragover` までは来るので気づきにくい）。
→ `dragstart` は `effectAllowed="copyMove"` にしてある。片方だけに狭めないこと。

---

## 前回見つかっていた小さなバグ

書き直しの過程ですべて解消した。同じ穴を開け直さないための覚え書き：

- 設定の「遅れ」件数は、一覧と同じ数え方に揃えた（毎日のやりたいことは数えない）
- `doneAt` を持たないデータでも「今日やったこと」の判定が壊れないようにした（`doneOn()` に集約）
- 日付・時間帯のまたぎで再描画するようにした（15秒ごとの確認 ＋ `visibilitychange`）
- 未使用だった `links` フィールドは移行時に落としている
- `philoIdx` のはみ出し補正は `save()` するようにした

---

## git の状態

`main` は `94893b7`（レイアウト刷新・箱モデル・画像・キャッシュ対策まで入っている）。

**main に入っていないもの（2026-08-09 時点）：**

| ブランチ | 中身 | 状態 |
|---|---|---|
| `claude/task-multi-link-app-launch-c21a9l` | 1つのやりたいことに複数リンク＋アプリ起動（v6） | 動作確認ずみ。保留中 |
| `claude/development-ideas-memo-qnflj5` | `IDEAS.md`（日記機能の案） | メモのみ。コードは触っていない |
| `claude/diary-feature-draft-den6gd` | **日記機能（Phase 1）＋ `IDEAS.md`** | 2026-08-13。ブラウザで動作確認ずみ。本人の確認待ち |
| `claude/timer-gratitude-tab-w8y9qn` | 同期の仕様（`SPEC.md` / `HANDOFF.md` の書き足しのみ。コードは触っていない） | 2026-08-14 |

**タイマー（音の差し替えつき）と日記の「日常に感謝」欄は、2026-08-14 に `main` へ入れた**（版 `2026-08-14.2`、`d4fdf80`。本人の指示で直接 push）。
**実際に耳で音を聞くのだけは、まだ実機で確認していない**（作業環境に音が無いため）。

**main に取り込みずみで、消してよいブランチ：**
`claude/layout-box-model` / `claude/pages-cache-update` / `claude/repository-review-a7i5bi`

- リモート：`nikunokuni/iamidle`（接続ずみ）。既定ブランチは `main`
- README は現状置いていない。必要になったら作る
- **作業を始める前に `git fetch origin` すること。** ローカルの `main` が古いまま
  「main には入っていない」と誤認しかけた（実際には入っていた）

---

## ファイル

| ファイル | 中身 |
|---|---|
| `index.html` | 画面の骨組みだけ。`style.css` と `app.js` を読む |
| `style.css` | 見た目。2カラムの指定とダークテーマ |
| `app.js` | ロジック全部。上から storage → 箱 → 頻度 → 描画 → 設定 → キーボード → boot |
| `SPEC.md` | 箱モデルの確定仕様。**本人と合意済み。勝手に変えない** |
| `IDEAS.md` | 開発案の置き場。**決定事項ではない。** 決まったものは実装して、ここに決定を書き足す |
| `HANDOFF.md` | このファイル |
| `sync/schema.sql` | 同期用。Supabase に1回貼って実行するもの（2026-08-14） |
| `sync/key.html` | 合言葉から「入口用の値」を作る道具。計算はブラウザの中だけ。**作り方は `app.js` 側と同じにすること** |
| `sync/README.md` | 同期の手順（SQL・合言葉・Vercel の環境変数・通じるかの確認） |
| `api/sync.js` | Vercel の Function。**中身は暗号文のまま預かるだけで、読めない** |

---

## 相手について

- 仕様を自分で詰めるタイプ。メリットとデメリットを自分で言語化してから相談してくる
- 判断は本人がする。選択肢と推奨を出せば決められる。**勝手に仕様を広げない**
- 「まだ実装しない」と言われたら、本当に実装しないこと
