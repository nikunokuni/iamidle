# この物置での作業の約束

## git

- **`main` に直接プッシュしない。** 本人がそのつど「mainにプッシュ」と言ったときだけ。
  1回言われても、それはその1回ぶん。次からはまた聞くこと。
- ふだんは作業ブランチにコミットしてプッシュし、そこで止めて報告する。
- プルリクエストも、頼まれたときだけ作る。

## 読む順番

1. `HANDOFF.md` … 引き継ぎメモ。何をどう作ってあるか、直近で何を変えたか。
2. `SPEC.md` … 仕様。

## 言葉づかい

画面に出す言葉は**「やりたいこと」**。「タスク」とは書かない。
コメントやメモの中でも「やりたいこと」で通す。
ただしコードの中の英語の名前（`tasks` `taskId` `renderTasks` など）はそのまま。
`S.tasks` は localStorage に保存されている形そのものなので、変えると既存のデータが読めなくなる。

## 作り

`index.html` / `style.css` / `app.js` の3ファイル。ライブラリなし、ビルドなし。
`index.html` を開けば動く。同期のために `api/sync.js` と `sync/` がある。
